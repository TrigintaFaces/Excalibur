# Sites that still derive a message identity from the CLR type instead of the declared name.
# SHRINK-ONLY: entries may be removed as they are fixed; a new one fails the gate.
# Each is tracked for burn-down -- see the message-identity beads.

# Routing name, not a stored event identity: MessageTypeCache.GetTypeName feeds
# context.SetMessageType for dispatch routing. Whether any of it reaches durable storage
# via the dead-letter path is an open question tracked separately -- baselined so the
# contract-return arm can be enforced now, NOT because this file has been cleared.
src/Dispatch/Excalibur.Dispatch/Caching/MessageTypeCache.cs
