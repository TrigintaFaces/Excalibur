# gate-wiring-baseline.txt — accepted known-orphan gates (tracked debt).
#
# Each line is the basename of an enforcement gate under eng/ci or eng/hooks that currently has NO
# caller running it in CI or a git hook. gate-wiring.sh accepts these instead of failing the whole
# battery, so the check ships LIVE against a codebase that already carries this debt.
#
# THIS FILE IS A SHRINK TARGET. Wiring a gate into a caller (a workflow, a git hook, or
# harness-gates-ci.sh) should DELETE its line here. A brand-new gate added with no caller is NOT
# allowed to be appended here casually — the default expectation is that a new gate is wired; a
# baseline entry is an explicit, reviewed admission of temporary debt.
#
# Format: one gate basename per line. Blank lines and #-comments ignored.

# --- enforcement gates that exist but are not yet run by any CI job or hook ---
no-internal-refs-gate.sh
tracked-artifact-gate.sh
# real committed-content enforcement gate that currently REFUSEs (cannot render a verdict, exit 2): its
# shipped-DDL-table -> src-write-path map does not yet cover ~18 shipped-DDL blocks, and its column-count
# floor rejects legitimate low-column keyed tables. Wiring it blocking today would red CI on a REFUSE, not
# a real defect; an advisory `|| true` wrapper would swallow the REFUSE into a false-green. Baselined as
# explicit tracked debt until the map + floor are completed, then wired blocking and this line deleted.
shipped-ddl-sweep.sh

# committed deliberately as a MANUAL tool, not as an enforcement gate. Its own header says so at
# line 5 ("STATUS: UNWIRED. NOTHING INVOKES THIS SCRIPT") and the integrator ruled it that way when
# it landed: it is a diagnostic a human runs, and labelling it in place was chosen over wiring it so
# that nobody reads its presence as enforcement. This line records that ruling where gate-wiring
# reads it. DELETE this line if it is ever given a caller — and update the header block at the same
# time, because a stale "UNWIRED" notice on a wired gate is the same lie in the other direction.
assert-binary-matches-source.sh

# --- beads/metrics + planning helpers invoked manually or only when their own file is staged ---
# manual human-decision triage tool (emits a terminal disposition for a human to act on); it evaluates
# nothing on src/ and its name is a forbidden workflow token, so it is structurally un-wireable as a
# committed-content gate.
premise-triage.sh

# --- non-.sh gates, visible since the enumeration widened to .py/.ps1 ---
# These were always orphaned; the meta-gate could not see them, because it enumerated by file
# extension rather than by what an enforcement gate is. Widening the population also WIRED three
# others (the workflow-inventory check, the shipping-filter validator and the repository-link
# validator), which is why they are absent from this list. The three below are not enforcement.

# A doc-surface extractor used as a library by the docs validation flow, plus a diagnostic a human
# runs. Already tracked as part of the wider set of gates that left the commit path and now run
# nowhere; recorded here so the meta-gate stays live rather than red.
docs-csharp-extract.py

# Reads the P0 issue set on stdin and prints a machine-readable denominator. Its only caller is an
# agent-mesh harness script, which is not a CI surface and does not run where CI runs. Wiring it into
# a workflow would be wrong: it classifies issue-tracker state, not repository state.
p0-classify.py

# A generator, not a gate: it renders release notes from git history. It has no --check mode, so
# there is no verdict for a caller to enforce. If it ever grows one, wire it and delete this line.
generate-changelog.ps1
