# Gitleaks Ignore File
#
# IMPORTANT: False-positive allowlists are now managed in .gitleaks.toml using
# path-regex and secret-regex rules instead of brittle filepath:ruleId:line entries.
# See .gitleaks.toml for all current allowlist entries and their justifications.
#
# This file (.gitleaksignore) is kept for emergency overrides only.
# To add a new false positive, update .gitleaks.toml instead.
# Ref: bd-pty3iu (Sprint 819)
#
# To generate a finding report for investigation:
#   gitleaks detect --source . --no-git --report-format json --report-path findings.json -v
#
# End of file
