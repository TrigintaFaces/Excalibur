<!--#if (UseAzure) -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AzureFunctionsVersion>v4</AzureFunctionsVersion>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Floats to the newest 10.0.0 PRERELEASE, which is what is published today. A bare
         10.* asks for >= 10.0.0 STABLE, which does not exist yet, so a fresh scaffold fails
         restore with NU1102 before the consumer has written a line of code. Change this to
         10.* AT the 10.0.0 stable cut - not before - so a scaffold then stops resolving
         prereleases. -->
    <ExcaliburDispatchVersion Condition="'$(ExcaliburDispatchVersion)' == ''">10.0.0-*</ExcaliburDispatchVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Excalibur.Dispatch" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Hosting.AzureFunctions" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Observability" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker" Version="2.*" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Extensions.Http" Version="3.*" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Extensions.Http.AspNetCore" Version="2.*" />
    <PackageReference Include="Microsoft.Azure.Functions.Worker.Sdk" Version="2.*" OutputItemType="Analyzer" />
    <!--#if (UseKafka) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.Kafka" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseRabbitMQ) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.RabbitMQ" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAzureServiceBus) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AzureServiceBus" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAwsSqs) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AwsSqs" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseGooglePubSub) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.GooglePubSub" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
  </ItemGroup>

  <ItemGroup>
    <None Update="host.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="local.settings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
    </None>
  </ItemGroup>

</Project>
<!--#elif (UseAws) -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Library</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <GenerateRuntimeConfigurationFiles>true</GenerateRuntimeConfigurationFiles>
    <AWSProjectType>Lambda</AWSProjectType>
    <!-- Floats to the newest 10.0.0 PRERELEASE, which is what is published today. A bare
         10.* asks for >= 10.0.0 STABLE, which does not exist yet, so a fresh scaffold fails
         restore with NU1102 before the consumer has written a line of code. Change this to
         10.* AT the 10.0.0 stable cut - not before - so a scaffold then stops resolving
         prereleases. -->
    <ExcaliburDispatchVersion Condition="'$(ExcaliburDispatchVersion)' == ''">10.0.0-*</ExcaliburDispatchVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Excalibur.Dispatch" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Hosting.AwsLambda" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Observability" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Amazon.Lambda.Core" Version="2.*" />
    <PackageReference Include="Amazon.Lambda.Serialization.SystemTextJson" Version="2.*" />
    <PackageReference Include="Amazon.Lambda.APIGatewayEvents" Version="2.*" />
    <PackageReference Include="Amazon.Lambda.SQSEvents" Version="2.*" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="8.*" />
    <PackageReference Include="Microsoft.Extensions.Logging" Version="8.*" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="8.*" />
    <!--#if (UseKafka) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.Kafka" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseRabbitMQ) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.RabbitMQ" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAzureServiceBus) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AzureServiceBus" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAwsSqs) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AwsSqs" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseGooglePubSub) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.GooglePubSub" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
  </ItemGroup>

</Project>
<!--#else -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Floats to the newest 10.0.0 PRERELEASE, which is what is published today. A bare
         10.* asks for >= 10.0.0 STABLE, which does not exist yet, so a fresh scaffold fails
         restore with NU1102 before the consumer has written a line of code. Change this to
         10.* AT the 10.0.0 stable cut - not before - so a scaffold then stops resolving
         prereleases. -->
    <ExcaliburDispatchVersion Condition="'$(ExcaliburDispatchVersion)' == ''">10.0.0-*</ExcaliburDispatchVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Excalibur.Dispatch" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Hosting.GoogleCloudFunctions" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Excalibur.Dispatch.Observability" Version="$(ExcaliburDispatchVersion)" />
    <PackageReference Include="Google.Cloud.Functions.Framework" Version="2.*" />
    <PackageReference Include="Google.Cloud.Functions.Hosting" Version="2.*" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="8.*" />
    <!--#if (UseKafka) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.Kafka" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseRabbitMQ) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.RabbitMQ" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAzureServiceBus) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AzureServiceBus" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseAwsSqs) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.AwsSqs" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
    <!--#if (UseGooglePubSub) -->
    <PackageReference Include="Excalibur.Dispatch.Transport.GooglePubSub" Version="$(ExcaliburDispatchVersion)" />
    <!--#endif -->
  </ItemGroup>

</Project>
<!--#endif -->
