# Retrospective — Sprint 852 (Reliability, Telemetry & Security Hardening + Advertised-Unwired Decompose)

**Date:** 2026-06-26 · **Mission:** 200 · **Closeout:** `4f59f0c64` · **Scope mode:** Hold-Scope (degraded-then-recovered roster)

## Commit Analysis
- **23 commits** in range (`b20fe65fa^..HEAD`), **+4561 / −2519**.
- By category: fix 7, feat 6, docs 4, test 3, chore 2, refactor 1.
- Notable separate commits: keystone 14z4ao staged (121e2cdbf → ebe01f291 → ed8926d1c), dz4itx clock-skew safety fix (5d028bf19), DOCS (4747bb373), PublicAPI promotion (cf1367265, operator-directed), closeout (4f59f0c64).

## Sprint Metrics
- **Planned:** ~28 beads / 7 lanes (degraded-roster Hold-Scope). **Closed: 15. Carried: 11 → S853.**
- Closed: 6v2z7q, w4m560, jj9gon, qu3182, a9adcc, b64hci, b5lr6q, ot72w3, dz4itx, 8o3c3p, **14z4ao (keystone, all 6 CDC providers)**, 18lb0t, 8f5361, d4o03t, lzx2cg.
- Carried→S853 (tracked open): umemwa, vfssk3, r4nd4w, rqntzf, kxexrz, o39j4u, pxhqri (P1), 1dq3my, bpgznn, 7vbvg1, bsglfv.
- Added mid-sprint (correctly tracked): dz4itx (self-caught clock-skew safety fix), SendingMessageCount→18lb0t (folded), + the S853 carries.
- **Verification:** TEST full 10-shard GREEN (~59,900, 0 regressions) · VERIFY 100% MUST, 0 orphans, AC-F4 closed · SAMPLES 92/92 · REVIEW_CODE + REVIEW_ARCH 0-blocking · governance-stack PASSED · AOT IL2/IL3 = 0 (native-link waived = pre-existing host-toolchain pkj4sz).

## What Worked Well
- **Integration gate caught a shipped SAFETY bug (dz4itx).** ot72w3's gate was a stale-build false-pass that committed `ShouldRelinquish` as `&&` (clock-skew split-brain + broken grace backstop) instead of `||`. Caught at the pre-IMPLEMENT-completion working-tree check by **investigating `M`-files rather than dismissing them as CRLF**; clean-rebuild + RED-lock confirmed the `||` fix. Without that check, a split-brain bug ships.
- **Clause-4 carriers ×2 kept the pipeline alive through an overnight outage.** ProjectReviewer (REVIEW_CODE) + DocumentationWriter (DOCS) both terminated overnight; same-type sub-agents registered as the dead mailboxes, did genuine independent work (clean review + validated docs), completed the phase tasks. Resilience proven further: the first DOCS carrier died on a transient rate-limit → re-spawned; the DocWriter revived mid-carry → handled the race (carrier had already completed).
- **F-5 full-CI backstop caught 2 stale siblings** the per-bead sweeps missed (TypeResolver scan-gate, GetOutboxStatistics lease-SQL) — recurring value.
- **Keystone staged foundation-first.** 14z4ao: Stage-1 core lift → Stage-2a public classifier + 2 providers → Stage-2b 3 data-loss-sensitive providers via a fresh-context sub-agent (Backend's quality-valve, honored). The no-advance-on-fatal data-loss guard was verified **6 ways** (sub-agent + PM + SA, across both review phases).
- **Cross-owner pre-alignment.** SA self-corrected 3×, PdM split/withdrew, the OCE P3 got a double-owner ruling, S853 priorities pre-aligned (correctness > features) before planning.

## What Didn't Work
- **Stale-build shipped a safety bug (dz4itx) — 6th occurrence.** `dotnet build <testproj> --no-incremental` does NOT rebuild project-referenced impl DLLs, so a lock can pass against a stale impl. Every prior occurrence was a false-failure; this one shipped a *false-pass* of a safety bug. Strongest evidence yet for a mechanical gate.
- **Parallel-create dup (1erne9 vs SA's bpgznn).** The reviewer files findings during review (mandate); the PM filed a second bead after the ruling without checking. Deduped, but avoidable.
- **Staging slip (cf1367265).** A `git add $(…)` + unconditional commit swept `.beads` + validation-results into the PublicAPI-promotion commit despite a "FOREIGN staged" warning — the commit wasn't gated on the check. Fixed-forward with a gated closeout commit; content correct, boundaries muddied.

## Test Failure Triage
- In-branch: 2 F-5 stale siblings (fixed in TEST) + EventSourcing timing-flakies (made deterministic, no-flaky directive). Pre-existing: AOT native-link (pkj4sz, waived). No unresolved failures.

## Recommendations
1. **[CHANGE — highest impact] Clean-rebuild the IMPL project explicitly before trusting any lock/test result.** `dotnet build <implproj> --no-incremental` THEN `<testproj> --no-incremental` THEN `--no-build` test. Evidence: 6th stale-build occurrence, this one shipped a clock-skew safety bug (dz4itx). **Promote to a rule + propose a mechanical pre-commit/CI gate.**
2. **[CONTINUE] Clause-4 carriers for terminated phase owners.** Evidence: REVIEW_CODE + DOCS delivered through an overnight 2-reviewer outage, incl rate-limit-retry + revived-owner-race handling.
3. **[CONTINUE] Integration-gate working-tree scrutiny — investigate `M`-files, never dismiss as CRLF.** Evidence: caught dz4itx in committed HEAD.
4. **[CHANGE] Gate the commit on the staged-check.** Never run `git commit` unconditionally after a "FOREIGN staged" warning; branch on it. Evidence: cf1367265.
5. **[CONTINUE] Reviewer-files-findings → PM checks before dup-filing.** Evidence: 1erne9/bpgznn dedup.

## Operator Action
- **Manual restart recommended for S853 full roster:** ProjectReviewer + DocumentationWriter idled overnight (both back online by close, but verify at S853 dispatch).
- **CEO competitive audit (operator-requested) landed in backlog:** 71 audit-2026-06 beads (8 severe P1 correctness) + 13 differentiation/vision epics — **operator schedules** the strategy epics; the severe correctness P1s feed S853 (PdM-ranked above features).
