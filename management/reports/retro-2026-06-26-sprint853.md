# Retrospective — Sprint 853 (Integration Correctness Hardening + S852 Carries)

**Date:** 2026-06-27 · **Mission:** 201 · **Closeout:** `6adf70b78` · **Scope mode:** Selective Expansion (correctness-first)

## Sprint Metrics
- **Planned:** ~26 beads. **Closed: 15.** Carried→S854: 5 sprint carries + 16 review/arch findings + ReactivateKeyAsync = 22 tracked-open.
- **Closed (15):** e1tsq2+skl8r7 (saga keystone, `9429bad33`+Firestore `c2e07d959`), vihqw6 (`8cb62a032`), o39j4u (`666084d8e`), r4nd4w (`e03fc82c5`), bpgznn (`28f4c9a11`), kxexrz (`142ab9bb8`), rqntzf+umemwa (`cec10d1bd`), v8k7jo (`269866b26`), d7ubqx (`bde17816c`), 60460q (`401d4ecf0`), grjjz0-FR1 (`b024f965a`), jqlqc8 (`2d38d26ef`), sklt7i (`432f2c315`). + ADR-340 (`76fba9414`, j26ldt ratified).
- **Verification:** TEST full 10-shard CI **119,572+ passed, 0 regressions**; VERIFY 15/15 MUST, 0 orphans; SAMPLES 92/92; REVIEW_CODE + REVIEW_ARCH PASS; governance-stack PASS; validate-samples SUCCESS; AOT IL2/IL3=0 (native-link waived = pre-existing host-toolchain pkj4sz).

## What Worked Well
- **Real-infra verification caught TWO mock-hidden bug classes at TWO gates.** (1) The saga integration-class **conformance** caught **5 bugs** the unit mocks (74/74) all passed: Mongo `DateTimeOffset`→BSON `Convert` (every save threw on real Mongo), `LoadAsync` version-restore missing in Mongo/Cosmos/Firestore (load-modify-save false-conflict), + no-resurrect. (2) **REVIEW_CODE source-read** caught the **vihqw6 Vault** security bug (`min_encryption_version=LatestVersion+1` rejected by real Vault → `SuspendKeyAsync` threw on its happy path = suspended key still usable; a mocked unit test certified it green). Same class, two gates, one sprint. → **promoted to a rule.**
- **Scope enumerated to real extent — twice — within the keystone.** 3→5 saga providers (optimistic-concurrency), then the 3-of-5 version-restore cross-provider pattern (caught by proactive code-read before CRUCIBLE).
- **vihqw6 grounded resolution (≈30 msgs, exemplary).** Altitude-check confirmed Vault has no native server-side suspend; ACL-balloon rejected (least-privilege); AC self-corrected (server-side→framework-level GetKey*) on Backend's "no encrypt path / no Vault crypto component" seam facts; semantic settled on **cross-provider consistency** (Suspended block-both, matching Azure/AWS/InMemory); reversible-vs-crypto-shred distinguished; fix kept contained (reuse existing `KeyStatus` enforcement) + a **NON-SKIPPED real-Vault lock 18/18**.
- **mtime-gate prevented a false-death.** Tests read `offline` (heartbeat frozen 16 min) while deep in a real-Vault container + RED-proof-mutant long turn — the mtime/known-long-op check correctly nudged (not clause-4'd); Tests revived. Validated the discipline.
- **Clause-4-vs-revived-owner race handled.** ProjectReviewer revived as its clause-4 carrier launched; carrier stopped cleanly, real owner kept REVIEW_CODE. No duplicate.
- **F-5 full-CI backstop** caught 3 stale saga siblings the per-bead sweep missed (impls correct; flipped to the new contract).
- **PM kept the team fed** after the operator flagged an idle window (the next lock batch hadn't been dispatched promptly once Tests freed up post-keystone).

## What Didn't Work
- **A non-functional security fix reached committed mainline (vihqw6 `11804f02c`)** before REVIEW caught it — a mock certified it. The re-fix was clean, but the lesson is the mock-not-real-infra gap (now a rule).
- **PM idle-window slip** — after the saga keystone, the persistence/LE lock batch wasn't dispatched to Tests promptly; the operator noticed the idle workers before I re-fed them.
- **SA posted the REVIEW_ARCH verdict but went idle before completing the phase task / closing j26ldt** — a brief stall (my nudge crossed its completion). Phase owners must complete the task, not just post the verdict.

## Recommendations
1. **[CHANGE — promoted to rule] verify-against-real-infra-not-mock** — `.claude/rules/process/verify-against-real-infra-not-mock.md`. Security/correctness fixes against external systems with server-side validation (or document-store round-trips) require a NON-SKIPPED real-infra integration lock; a mocked-client/unit lock certifies a broken provider. Evidence: 5 saga + vihqw6, 2 gates, 1 sprint.
2. **[CONTINUE] mtime-gate before clause-4** — distinguished a 16-min real-Vault long turn from a death. Keep.
3. **[CONTINUE] scope-keystone-real-extent + verify-real-seam-exists** — caught the 3→5 provider expansion and the "no Vault crypto component" fact that reshaped the vihqw6 fix.
4. **[CONTINUE] F-5 full-CI backstop** — 3 more stale siblings caught (recurring value).
5. **[CHANGE] PM dispatch the next lock batch the instant the prior phase frees the author** — avoid the idle window (operator-caught this sprint).

## Carried → S854 (tracked OPEN)
- Carries: grjjz0-FR2 (5gtfje), fro108, vfssk3, umemwa-5-LE, 1dq3my.
- REVIEW_CODE (10): 762uzn/no0lue (P1) + pg79ni/4desh8/msmwr9/8aef8d/axdtve (P2) + 1f5om2/5jo6tm/7npc0q (P3).
- REVIEW_ARCH (6 non-blocking) + ReactivateKeyAsync.

## Operator Note
- ProjectReviewer revived this sprint (clause-4 carrier stopped) but had been offline ~2h before — flag for restart-stability. Full roster otherwise healthy.
