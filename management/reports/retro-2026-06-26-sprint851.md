# Retrospective — Sprint 851 (Reliability, Telemetry & Security Hardening)

**Date:** 2026-06-26 · **Mission:** 199 · **Closeout:** `ba190651b`

## Commit Analysis
- **33 commits**, 119 files, **+4612 / −389**.
- By category: test 10, fix 7, docs 6, feat 5, chore 4, refactor 1.

## Sprint Metrics
- **Planned:** 28 beads / 8 lanes. **Closed: 25 (89%). Carried: 3** (kxexrz, 14z4ao, yd6dsj — PM-ruled mid-sprint, seams pinned).
- **Added mid-sprint (correctly tracked):** c6wd6f (self-introduced wpynky P1, fast-follow) + ~12 S852 follow-up beads (w4m560, qu3182, 6v2z7q elevated, SA's 8 non-blocking).
- **Verification:** full 10-shard CI GREEN · REVIEW_CODE CLEAN (0 blocking) · REVIEW_ARCH APPROVED (0 blocking, boundary 0 violations, CSO report) · DOCS build PASS · governance + samples (92/92) PASS · AOT IL-compat clean (native-link waived = pre-existing host-toolchain).

## What Worked Well
- **Integration-gate re-verify caught a shipped regression.** The PM gate's real-indicator re-grep found CdcJobQuartz + 11 more ES samples silently bricking — `build-0/0 ≠ samples-run`. Without it, 12 bricking samples ship.
- **Clause-4 carrier resilience.** 3 windows terminated overnight; REVIEW_CODE + DOCS were carried end-to-end by same-type sub-agents (registered as the dead mailboxes, did genuine independent work, completed phase tasks → mission advanced). REVIEW_ARCH ran on the live SA. Pipeline never stalled; PM retained all git.
- **Microsoft-bar fast-follow over deferral.** wpynky's secure-but-unusable gap was surfaced at SAMPLES and fixed *this sprint* (c6wd6f) rather than shipping a known regression — "build the fix."
- **Grounded disposition over assumption.** CdcJobQuartz local-vs-framework-serializer ambiguity resolved run→read→cite (it genuinely bricks via framework `AddEventSourcing`→`ResolveType`), not guessed.

## What Didn't Work
- **Proxy-grep enumeration under-scoped the keystone.** The brick set was enumerated by `JsonEventSerializer` references (10) instead of the real indicator `AddEventSourcing`/`IEventSourcedRepository` (22). Caught at the integration gate, but late — should be an IMPLEMENT-phase habit.
- **F-5 narrowness recurred at finer grain.** qu3182: the retry-exhaustion metric covers the result-abandoned path but not the exception-exhaustion path — F-5 must enumerate both *code paths* within a surface, not just both *surfaces*.

## Recommendations
1. **[CONTINUE]** PM integration-gate re-verify by the *real failure indicator*, never a proxy. Evidence: caught the 12-sample c6wd6f regression.
2. **[CHANGE]** Enumerate blast-radius by the real indicator at IMPLEMENT, not at the gate. Evidence: c6wd6f 22-vs-10. Bake into S852 PLAN.
3. **[CONTINUE]** Clause-4 carrier-spawn for terminated phase owners. Evidence: REVIEW_CODE + DOCS delivered despite 3 dead windows.
4. **[CHANGE]** F-5 sweep enumerates both retry surfaces AND both exhaustion code paths. Evidence: qu3182.
5. **[TRY]** Spec a "secure default must stay usable" check at SPEC for any security-hardening bead. Hypothesis: catches wpynky-class secure-but-unusable gaps before SAMPLES.

## Test Failure Triage
- In-branch: 1 (DefaultRetryPolicy F-5 sibling — fixed in TEST). Pre-existing flaky: 2 (ncmywy, tracked). AOT native-link: pre-existing env (pkj4sz). No unresolved failures.

## Operator Action
- **3 windows need manual restart for S852:** FrontendDeveloper, ProjectReviewer, DocumentationWriter (idled out overnight).
