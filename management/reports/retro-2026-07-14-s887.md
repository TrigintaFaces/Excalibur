# S887 Retrospective — Provider Correctness & Governance Cleanup

**Mission 235 · SHIPPED @ `4070d3653` · 2026-07-14 (autonomous overnight ops)**

## Commit Analysis
- **78 commits**, +6,585 / −883 lines, since baseline `4b284ceb`.
- By category: chore 42 (54%), fix 19 (24%), test 7 (9%), docs 6 (8%), feat 4 (5%).
- The high chore ratio = a correctness/governance-cleanup sprint (integration + tracker-sync + remediation), not a feature sprint — expected for the theme.

## Sprint Metrics
- All 12 mission phases green (STRATEGY skipped — continuation sprint, no new bet).
- Keystones delivered: Oracle/multi-provider event-store/inbox/outbox correctness, CDC single-active-consumer + fencing, durable `Workflows.SqlServer` signal inbox, canonical serializer, tenant-scope inbox isolation, ftwim0 sweep.
- **Open P0 = 0.** in_progress = 0 at close (no ambiguous claims).

## Quality Gates (all PASS)
- Full CI **10/10 shards, ~112,575 passed / 0 failed / 63 tracked skips**
- VERIFY full-coverage, 0 critical gaps · SAMPLES **93/93** · AOT **0 IL warnings** · governance-stack PASS

## What Worked Well
1. **Independent REVIEW_CODE earned its keep.** SENTINEL caught **2 BLOCKING + 2 P1 CI-invisible defects in the sprint's OWN keystones** that TEST (10/10) and full-coverage VERIFY both passed: CDC mid-batch-demotion NULL fencing-token → split-brain checkpoint corruption; Postgres inbox unconditional `tenant_id` → non-MT `42P10` + silent exactly-once bypass. Both are the recurring **"green gates miss the seam defect"** class — the reviewer reading impl on committed mainline (not diffs/green) is the net that caught them. Already covered by `enforce-invariants-structurally` + `verify-against-real-infra-not-mock` + `gate-full-guard-suite`.
2. **Grounded challenge corrected a PM/reviewer ruling before it caused rework.** Tests flagged that the reviewer's "sweep all 3 pair-PK fixtures → triple key" was *contradicted* by Backend's conditional fix (non-MT stays pair-key; sweeping → `42P10`). PM retracted the ruling (verify-before-claiming: the instruction was a stale-premise claim). `speak-up-challenge-instructions` working as designed.
3. **HIVE mesh + mtime-gate liveness held gapless** through an autonomous overnight run. DocWriter's ~40-min DOCS turn read `offline` on OPCOM heartbeat but recent docs-file mtimes proved it alive → correctly NOT woken (no collision). SA's "CDC still uncommitted" wake was a benign observation of the working tree *during* a commit-timeout window — resolved by a HEAD re-verify, not a re-commit.
4. **Coupled impl+lock integration per lane** (author≠impl locks RED-on-pre-fix → GREEN) kept committed mainline honest; SENTINEL + ORACLE both re-passed on committed HEAD.

## What Didn't Work / Friction
1. **Integration-commit hooks time out under Bitdefender.** The CDC coupled commit hit the 2-min tool limit mid pre-commit hook (exit 143); HEAD had not moved, staged set intact → clean re-commit with a 300000ms timeout. Cost: one wasted attempt + a HEAD verify. Mitigation already in the loop directive (300000ms for commits).
2. **`blocking-bead-gate` prints "N live P0(s)" that is inert AND miscounted** — reported "1 live P0" every commit while `bd list --status open --priority 0` = **0**. Noise that invites a false carryover. Non-blocking (the gate enforces nothing), but the count is wrong.

## Test Failure Triage
- In-branch (caused by sprint work): 2 (shard-10 RS0030 F-5 under-sweep; shard-2 erasure stale sibling) — **both fixed** in `9d67a6020`.
- Pre-existing / flaky: 0 new. 63 skips are pre-existing tracked (CloudEvents `bd-jj4hx4`, at-least-once `bd-5dox7c`).

## Recommendations
1. **[CONTINUE]** Independent reviewer reads impl on committed mainline, not green gates. Evidence: 4 CI-invisible own-keystone defects caught post-VERIFY this sprint. This is the single highest-leverage gate.
2. **[CONTINUE]** mtime-gate before waking a phase owner. Evidence: DocWriter's long DOCS turn correctly not-collided; SA's timeout-window wake correctly resolved by re-verify.
3. **[TRY]** Fix `blocking-bead-gate`'s P0 count (or silence it when 0 real open P0s). Hypothesis: removes recurring false-carryover noise. Measurement: gate line matches `bd list --status open --priority 0`. (File as a P3 harness bead, not this sprint.)
4. **[CONTINUE]** 300000ms commit timeout under Bitdefender — already adopted.

## Rule-Promotion Verdict
**0 new rules.** The headline finding (green gates miss the seam defect) is already covered by `enforce-invariants-structurally`, `verify-against-real-infra-not-mock`, and `gate-full-guard-suite` (rule-promotion-gate: default NO; overlap found). The Bitdefender-commit-timeout and blocking-bead-gate-miscount are single-occurrence env/tooling items → track, don't promote.
