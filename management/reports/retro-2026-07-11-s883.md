# S883 Retro — Tracker & Gate Integrity (DEBRIEF)

**Mission 231 · SHIPPED @ `cf36ab033` · 2026-07-11**
Scope-reduction integrity sprint. 14 beads closed, 7 commits (`873b6689f`→`cf36ab033`), 0 blocking findings across the entire pipeline (TEST/VERIFY/REVIEW_CODE/REVIEW_ARCH/governance all PASS).

## Outcome
Fixed the issue-tracker + CI-gate substrate that S882's retro named "the top recurring cost": honest cross-shard CI aggregation (set+compile+dedup, non-vacuity floor), lossless bd comment/description durability + clobber guard, single-daemon determinism + write-effect read-back (`mhbin6`/`svacnv` exit-0-no-op class closed), 4 stale-P0 verify-closes, and `tpu8m2` relocated off product-CI. Every deliverable shipped impl + non-vacuous safety+liveness self-test + independent author≠impl lock.

## What went well
- **The tracker fix proved itself on itself.** The bd-durability guard gated the very commits that wrote the bead-closes to git (DB→jsonl→committed HEAD, 0 drops). Strongest possible validation of the sprint thesis.
- **Grounded challenge caught every drift before it cost real work:** SA's first meta-gate cut would have violated the operator's harness-off-CI directive — my STOP-condition caught it. My IMPLEMENT dispatch ran on a stale GUIDE doc — SA caught it (crossed decomposition) before Backend built the wrong thing. The `tpu8m2` "LIVE" premise was stale — SA verified the CR fix was already at HEAD. TestsDeveloper refused to burn hours on a no-signal full-CI. Each was a friendly, evidence-cited challenge resolved by the decision owner.
- **Scope-reduction discipline held.** Operator chose "small hardening" over the ~100-bead budget; the team deferred `ud36ow`/`jxp2yq`/`458ahs`/`4uubd1` to S884 rather than scope-creeping.
- **Zero foreign contamination across 7 commits** despite a working tree full of pre-existing foreign uncommitted work — via surgical hunk-staging (`git apply --cached` for the `ci.yml` aggregation hunk) and explicit-path staging throughout.

## What was hard / lessons (mostly covered by existing rules)
- **bd non-determinism bit the workflow twice** — a `bd update -a` at dispatch and a `bd update --status closed` at CLOSE (`2t6lfx`) each exited 0 having changed nothing; caught by read-back + retry. This is *exactly* `svacnv`, the bug S883 fixed — dogfooding validated it. (Covered: `verify-before-claiming` exit-code-≠-effect.)
- **Stale installed `.git/hooks/pre-commit` broke the `tpu8m2` relocate.** `core.hooksPath=.git/hooks`, and the installed copy hard-referenced the moved gate's old `eng/ci` path → commit failed until the local path was fixed. This is the `verify-hooks-current` install-sync gap → **tracked, S884**.
- **A dirty working tree from prior sessions is a live integration hazard.** The pre-existing foreign `gate-selftests` `ci.yml` block + untracked `gate-wiring.sh`/`bd-status-tokens.sh` tangled with S883 the whole sprint; the block now references a file S883 *moved*, so committing it as-is breaks CI. Surgical staging + committed-HEAD verification (never trust the working tree) was essential. (Covered: `forge-integration-conventions`, `clean-rebuild-before-trusting-locks`.)
- **Zero-product-delta sprint → gate waivers.** Full-CI (10-shard), AOT, and samples verify product/build state S883 never touched; re-running them = hours of no-signal ritual. Operator waived full-CI; PM extended the same rationale to AOT/samples, kept the cheap governance-stack (which PASSED). Good precedent for pure-tooling sprints (Test Execution Protocol: run what the change touches).

## Rule-promotion gate (default: NO new rule)
Every recurring failure this sprint is already owned by an existing rule (`verify-before-claiming`, `forge-integration-conventions`, `coordinate-before-parallel-work`, `clean-rebuild-before-trusting-locks`). **No new rule promoted.** The one genuinely novel gap — the stale *installed* `.git/hooks/pre-commit` vs the tracked `eng/hooks/pre-commit` — is a mechanical/tooling fix (`verify-hooks-current`), tracked as an S884 bead, not a rule.

## Carryover → S884
`4uubd1` (blocked product test), `ud36ow` gate-honesty epic (+`jxp2yq` f5-sweep wiring, `458ahs`), `ltfo0a` P2 review nits, the foreign `gate-selftests` reconcile (re-point the stale `ci.yml` path before committing), `verify-hooks-current` install-sync gap. S884 = the deferred product/feature sweep on a now-trustworthy substrate.

## Post-ship action
Observe the new `unit-shard-aggregation` job GREEN on the integration-push CI (its wiring is testable only on GitHub Actions).
