# Retrospective — Sprint 844 (Transient Circuit-Breaker Dead-Letter Correctness)

**Date:** 2026-06-24 · **Author:** ProjectManager (DEBRIEF) · **Mission:** 192 · **Branch:** `chore/dev-team-six-dotnet-update`

## Commit Analysis

| | |
|---|---|
| Period | 2026-06-24 (single session, ~3h13m wall-clock: 16:26 plan → 19:40 close) |
| Total commits | 18 (`3672d8361` → `f49b747b2`) |
| Lines added / removed | +2715 / −1474 |

**By category:** spec 5 · test 3 · fix 3 · chore 3 · plan 2 · docs 2.
The 5 `spec` commits reflect MS-1's iterative honest-framing (CEO-condition folding → AC-7 2/9 framing → SQL-declined) — not churn-rework, but the spec evolving to match the honestly-discovered reality. (`6166a2abc` is an S843 carryover PublicAPI promotion that landed early.)

## Sprint Metrics

| | |
|---|---|
| Planned floor | 2 (`v9jq1a` Inbox, `2tvy5s` Outbox — deduped from 4; 2 auto-twins closed at PLAN) |
| Completed | **2/2 floor** + `u76sid` (folded P3) = **3 beads closed** |
| P1 | 2/2 closed (`v9jq1a`, `2tvy5s`) |
| P3 | 1/1 closed (`u76sid`, folded into CLOSE) |
| Carried over | `3rxrtw` (P2, 7-store TestContainers conformance infra) |
| Added mid-sprint | 2 — both legitimate: `u76sid` (honesty artifact of the routing flip) + `3rxrtw` (sanctioned conformance-infra deferral) |
| Blocked | 0 |

## Quality Trends

- **Full CI:** 10/10 shards GREEN, 0 regressions, 0 skipped. CI green is the headline.
- **Governance gates:** governance-stack ✅ · samples ✅ (92/92 build + 92/92 smoke) · AOT ✅ (0 IL2xxx/IL3xxx, Publish SUCCESS — vswhere PATH fix from S843 held, no MSB3073 recurrence).
- **Reviews:** REVIEW_CODE + REVIEW_ARCH both **0-blocking** (independent on committed HEAD); **CSO 0 security findings** (SQL parameterized).

## Pattern Analysis

**What Worked Well**
- **Seam-blocker resolved entirely PRE-CODE, zero rework.** The keystone (`v9jq1a`) hit a real blocker — InboxProcessor is *not* claim-based, so the architect's first `ReleaseAsync` pin was invalid. Caught by a grounded run→read→cite challenge *before* any implementation; re-ruled to a new `IInboxStoreAdmin.MarkFailedAsync(retryCount)` overload, then simplified (upsert→`UPDATE`-set-count) — the 21-file cross-store impl landed once, clean.
- **Verify-before-claiming caught a cascade of real issues** — the 2/9 AC-7 breadth (TestsDeveloper, *before* claiming done), the SQL-class infeasibility (no schema-init), the kit-conformance duplicate (588 vs 587), and **both reviewers'** LoggerMessage-template miss. Each surfaced honestly, not by a downstream failure.
- **Scope-the-keystone grounded challenge** — the real extent grew 12→20→21 files (8 public store classes each need a PublicAPI entry); caught + handled as a sanctioned missing-dependency, not silent scope-creep.
- **Honest retractions throughout** — SA's `ReleaseAsync` pin + the "stranding" rationale; ProjectReviewer's "orphaned mapping" lean (retracted on DocWriter's shipped-public evidence); the optional SQL class (declined on verified infeasibility). Every reversal evidence-based.
- **CEO bless via the liveness net** — the Hold-Scope public-API bless stalled on an idle CEO; the whois→nudge→operator-escalation process net (verify-worker-liveness) unblocked it, and the CEO ruled track-and-proceed honoring his own Microsoft-bar (works-everywhere ≠ document-the-limitation).
- **Tracked-not-buried** — `u76sid` folded (honesty), `3rxrtw` carried P2 (enumerated), the enum retained — every residual ruled or tracked.

**What Didn't Work**
- **Observability-template staleness — the one real gap.** The routing fix flipped CB-open from DLQ→leave-for-retry but left the `LogCircuitBreakerOpen` LoggerMessage templates (`InboxProcessor.cs:1011` / `OutboxProcessor.cs:1165`) asserting "routed to DLQ". Both REVIEW_CODE *and* REVIEW_ARCH verified the routing logic but did **not** read the template *strings* at the file bottoms (outside the diff hunks). Caught only at DOCS (DocumentationWriter). Folded + fixed (`u76sid`), but it should have been a SPEC-level AC. *Net cost: a CLOSE-phase fold; zero functional impact.*
- **AC-7 conformance shape churned** (standalone InMemory → kit relocation → duplicate → resolved) across several messages. Net functional cost zero, but real coordination — the shape wasn't pinned before authoring started.

**Surprises**
- The 7 DB inbox stores have **no `InboxStoreConformanceTestBase` class at all** — a pre-existing infra gap surfaced only by AC-7's "uniform all-9" ambition (→ `3rxrtw`).
- The keystone's real file extent exceeded the ratified file-map **twice** (12→20 PublicAPI; the seam itself).

## Test Failure Triage

```
In-branch (caused by sprint work):  0 — CI 10/10 green, no regressions
Pre-existing (already broken):      0
Flaky (intermittent):               0 — no flakes under full-shard load
Env (not a test failure):           0 — AOT vswhere PATH fix (S843) held
```

## Recommendations (ordered by impact)

1. **[CHANGE] A routing/behavior-change spec MUST include an AC to update observability template strings (LoggerMessage/telemetry) + their tests.**
   Evidence: `u76sid` — the CB-open routing flip left the log templates asserting the *old* behavior; both reviewers verified routing but missed the template text; caught at DOCS. Effort: low — a `spec-decompose` checklist line ("behavior/routing change → audit LoggerMessage/Activity template *strings* for staleness, add an observability-consistency AC"). *(Composes with the existing `enforce-invariants-structurally` family — telemetry strings are part of the contract a behavior change must update.)*

2. **[CONTINUE] Keystone-first + author≠impl RED-proven locks + verify-before-claiming + independent review on committed HEAD.**
   Evidence: zero rework on a 21-file cross-store keystone; the seam-blocker, the 2/9 breadth, the SQL-feasibility, the kit-duplicate, and the template miss were *all* caught by grounded run→read→cite rather than a downstream failure. Load-bearing — keep mandatory.

3. **[CONTINUE] Scope-the-keystone grounded IMPLEMENT challenge.**
   Evidence: the 12→20→21-file extent growth and the not-claim-based seam were both caught pre-code; the sanctioned missing-dependency path (PM scope ruling + CEO bless) kept it honest, not creep.

4. **[TRY] Blast-radius the *observability + docs + log* surface at SPEC, not just the code surface, for behavioral-routing changes.**
   Hypothesis: a SPEC-phase "what telemetry/docs/log strings describe this behavior?" grep removes the DOCS-phase stale-claim catches. Measurement: next routing-change sprint ships with log/docs already consistent (no DOCS-phase stale-claim fix).

5. **[CONTINUE] Tracked-not-buried under Hold-Scope.**
   Evidence: `u76sid` folded, `3rxrtw` carried P2 (enumerated), the shipped enum retained on evidence — nothing dropped, nothing rushed.

## Agent Breakdown

| Agent | Phase(s) | Beads | Highlight |
|-------|----------|-------|-----------|
| ProductManager | SPEC (BLUEPRINT) | — | 2 mini-specs; honest CEO-condition folding + AC-7 framing self-corrections; the docs-dependency pin that halted a rushed enum removal |
| SoftwareArchitect | GUIDE/REVIEW_ARCH (COMPASS/ORACLE) | — | seam pins + honest `ReleaseAsync`/stranding retractions; CSO 0-findings; grounded KEEP-the-enum ruling (~20-test evidence) |
| BackendDeveloper | Lane A (IMPLEMENT) | v9jq1a, 2tvy5s, u76sid | 21-file cross-store impl, build-green; the self-correction that re-grounded the driver (no-increment, not stranding); precisely-scoped u76sid |
| TestsDeveloper | TEST/VERIFY (CRUCIBLE/TRACEPOINT) | engage-tests | every author≠impl RED-proven lock; the 2/9-breadth + SQL-feasibility + kit-duplicate verify-before-claiming catches; spec-verify PASS |
| ProjectReviewer | REVIEW_CODE (SENTINEL) | — | independent 0-blocking on committed HEAD; honest symmetric self-correction + retraction on the enum |
| DocumentationWriter | DOCS (CHRONICLE) | u76sid (filed) | caught the stale log templates + docs; the shipped-public-enum docs-dependency pin |
| CEO | STRATEGY (NORTHSTAR) | — | GO 2/2 Hold-Scope + the 13→12 bless + the AC-7 track-and-proceed ruling honoring the Microsoft-bar |
| ProjectManager | PLAN/IMPLEMENT/CLOSE/RETRO | dedup + integration | 18 selective-staged commits; per-bead gated integration; the 3-grep all-9 AC-7 verification; CEO-liveness escalation |

**Praise:** TestsDeveloper + both reviewers — the verify-before-claiming culture caught every divergence (breadth, feasibility, duplicate, template text) before reality did. BackendDeveloper — the honest "my staleness, owned + fixed" on both the rationale self-correction and `u76sid`.

**Growth area:** the team (collective) — observability template strings need a SPEC-level AC on routing changes (rec #1); not a blame item (the self-corrections were exemplary), a process gap.
