# Retrospective — Sprint 897 (Mission 245)

**Period:** 2026-07-22 12:11 → 2026-07-23 13:00 (~25h, autonomous overnight). **PM/integrator:** ProjectManager.

## Commit analysis

| Metric | Value |
|---|---|
| Total commits | 188 |
| Lines | +13,484 / −2,936 (509 file-touches) |
| fix | 30 · **test** 22 · **docs** 55 · ci 5 · feat 3 · revert 1 · chore 72 (mostly per-close tracker syncs) |

Shape: a **P0-heavy correctness sprint** — tenant-isolation, GDPR erasure, and outbox durability dominated. High docs count reflects the guarantee-contract `ARCHITECTURE.md` discipline + an xml-doc-cref gate cleanup (51 broken crefs across 49 files resolved; gate armed). Low feat count is expected for a "release gate" sprint.

## Headline outcomes

- **n8ogfo — GDPR event-store erasure P0** (`1631e0b8b`): DI decorator chain stripped `IEventStoreErasure` → erase reported success while doing nothing. Fixed by chain-forwarding through every decorator + a fail-closed sharding-erasure guard. Design converged on **column-agnostic (A)** after four *evidence-grounded* owner flips, decided by an F-5 DDL enumeration proving a no-column event-store table is a supported, tested shape. Real-SqlServer 13/13, SA committed-HEAD re-verify PASS.
- **18c3el / kr7cyk / s7yc33** — event-store & snapshot untenanted-predicate correctness (8 leaks scoped + non-vacuous locks); Design-A None-path settled.
- **8rqzdl** — real-infra saga tenant-isolation locks across Mongo/Cosmos/DynamoDb/Firestore.
- **sicpvm / 604883** — durable SqlServer outbox fence (cleanup can't reopen the guard); DOCS later corrected the contract to match (`3d28edf14`).
- **Gate integrity** — xml-doc-cref-gate armed (`|| true` removed, backlog zeroed), AOT trim-warning gate now fails on unsuppressed IL warnings.

## What worked

- **Integrator full-suite re-run caught the P0 a green shard + 5 review passes missed.** The single highest-value practice of the sprint. `gate-full-guard-suite` + `clean-rebuild-before-trusting-locks` did exactly their job.
- **Evidence over authority.** Four owners (SA, ProductManager, Backend, PM) flipped their A-vs-B position on *verified DDL facts*, not rank. The decider question ("produce a no-column variant or concede") resolved a P0 design cleanly.
- **Real-infra locks (not mocks)** surfaced provider-specific truths (`verify-against-real-infra-not-mock`); the DDL enumeration + real-SqlServer/Postgres suites were decisive.
- **DOCS guarantee-contract sweep works** — caught the Outbox durable-fence *understatement* (a safety guarantee documented as best-effort) that nothing else would have.
- **Explicit-stage discipline** (never `-A`, exact coupled sets, clause-6 committed-HEAD read-back) landed every commit clean with no foreign-file bleed.

## What didn't work

- **Crossed-message thrash on the A-vs-B thread.** A (B) ruling was issued against a snapshot that crossed the architect's (A) convergence by 30 seconds; it cost a full retract/re-sync cycle. Root cause: a ruling issued without confirming it was synchronized to the latest owner position.
- **Real-infra integration tests escape the deterministic shard.** The 2 RED erasure tests sat on committed HEAD undetected because the deterministic CI shard doesn't run real-SqlServer integration — the P0 was invisible to the standard gate.
- **`blocking-bead-gate` is INERT** — 16 live P0s, none declare `BLOCKS-PATHS`, so the gate enforces nothing while printing a warning each commit. A control that manufactures the belief it's working.
- **Reconcile left 7 discovery beads open** (self-declared-resolved, no cited commit) — correct non-vacuous behavior, but the backlog carries unverifiable "done" claims that need manual triage.

## Test failure triage

- In-branch: the 2 n8ogfo/2g734k erasure REDs — **fixed** (converged to (A), 13/13 green).
- Pre-existing surfaced: 18c3el sibling predicates (7), several stale F-5 siblings — all fixed + locked this sprint.
- Flaky: none observed.

## Recommendations (ordered by impact)

1. **[CHANGE] Close the deterministic-shard-vs-real-infra coverage gap.** A real-infra integration RED must be visible on a gate, not only on an integrator's manual full-suite run. Evidence: n8ogfo P0 hid on committed HEAD past 5 review passes. Effort: medium (CI shard config). → next-sprint CI/CD theme.
2. **[CHANGE] Arm or retire `blocking-bead-gate`.** An inert gate is worse than none — it fabricates confidence. Evidence: 16 P0s, 0 BLOCKS-PATHS, warning ignored every commit. Effort: small.
3. **[CONTINUE] Integrator full-guard-suite re-run on the coupled tree, always.** It is the load-bearing net. Evidence: it caught the sprint's defining P0.
4. **[TRY] Timestamp-sync a ruling to the latest owner position before issuing it.** Hypothesis: eliminates crossed-message thrash on fast threads. Measurement: zero retracted rulings due to superseded snapshots next sprint. (Candidate for `/self-improving` — check overlap with `verify-before-claiming` first.)
5. **[CONTINUE] Guarantee-contract `ARCHITECTURE.md` sweep at DOCS.** Evidence: caught the Outbox understatement. Keep it.

## Self-improving seeds (for the `/self-improving` pass)

1. **Crossed-message thrash** — a ruling must be timestamp-synced to the latest owner position before it's issued. **Check `rule-promotion-gate` first:** recurrence ≥3×? overlap with `verify-before-claiming` (scope-from-secondary-evidence) / `coordinate-before-parallel-work`? Likely a **clause-add**, not a new file.
2. **Real-infra tests escape the deterministic shard** — this is a **gate/CI fix**, not a prose rule (`rule-promotion-gate §6`: ask where the gate is). Track as a bead, build the shard coverage; do not promote prose.
3. **DOCS guarantee-contract sweep** — already a working discipline; no promotion needed (don't tax the corpus for something that worked).
