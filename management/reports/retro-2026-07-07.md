# Retrospective — Sprint 877 (2026-07-06 → 07)

**Scope:** Durable-Execution W1 + GDPR Crypto-Shred + Multi-Tenancy W1/W2 + Provider W2 (band ~100, MVP-floor delivered).
**Outcome:** CLOSED, all gates GREEN. Closeout `e3eafc27a`.

## Commit Analysis
- **38 s877 commits** (integration + review-remediation phase): feat 3 · fix 11 · test 4 · docs 2 · chore 17 · ci 1.
  (Keystone IMPLEMENT commits landed earlier; this count is the integrator's review→CLOSE window.)
- Beads: 8554 closed cumulative · 204 open (backlog + carryover) · 62 blocked.

## Sprint Metrics
- **MVP floor + tenancy W1/W2 delivered** (durable-exec W1, crypto-shred core, ambient tenancy + store bridge, provider W2, A3 hardening).
- **Carried (tracked next-band):** hm0nfd W3 tenant-stores (8), durable-exec W2–W4 (12), uahb0i store-decorators (4), Spanner/Marten/Aspire providers, `lu2jdc` inert-signing.
- All gates: CI 10/10 (~59k) · governance PASS (380) · samples 93/93 · AOT 0-warn · REVIEW_CODE + REVIEW_ARCH PASS 0-blocking.

## Test Failure Triage
- **In-branch: 2** (F-5 `ErasureService.KeysDeleted` stale siblings; `TenantContextHolder._disposed` non-volatile) — both FIXED by the TEST full-CI backstop.
- **Pre-existing/flaky: 1** (`ExactlyOnceMeterCollectionShould`, passes isolated) — tracked, non-blocking.
- **moe5g9** (librdkafka native-thread leak) — FIXED via serial CI invocation for the Transport shard.

## What Worked Well
- **REVIEW→remediate→re-verify loop.** SENTINEL caught 2 BLOCKING advertised-but-inert false-safety controls (MQTT `UseSharedSubscription`, `RequireTenant`); every fix landed on committed HEAD + RED-first-locked before advance.
- **TEST full-CI backstop** caught 2 real regressions unit gates missed (the F-5 `KeysDeleted` contract change; the volatile-disposed conformance).
- **clean-rebuild-before-trusting-locks** caught 2 masked defects *pre-integration*: `8hkfol` shipped with a masked RS0016/RS0017 build-fail (incremental "0/0"); `so2rhi`'s committed fix had a `TryAddEnumerable`-factory throw exposed by Tests' independent DI lock.
- **Independent RED-first locks (author≠impl)** — Tests' locks caught real defects in *committed* fixes, not just green-washed the happy path.
- **verify-before-claiming** — held every fix's build/lock before citing; and SA's run→read→cite corrected my own mis-attribution.

## What Didn't Work / Recurring
- **ADR-336 advertised-but-unwired = the dominant defect class** (2 blockers + a HIGH inert cluster + `lu2jdc`). Public options advertising a guarantee the code doesn't deliver (false-safety). Recurring across sprints — caught only at REVIEW, not design/IMPLEMENT time.
- **F-5 cross-project sweep miss** (`vb6cwl` `KeysDeleted`) — a type-contract change whose sibling tests weren't swept at IMPLEMENT; caught late by the TEST backstop. Same class as the codified f5-cross-project-test-sweep rule.
- **CLOSE-gate nudge storm** — Task #2484/#2491 re-dispatched 30×+ during long integration/gate runs. 5th+ recurrence (bead `7h7srz`, S867/S874/S875/S876/S877). The client in_progress-heartbeat mitigation is proven insufficient.
- **Integrator (me) errors, both owned:** (a) I *restored* carried W3 files into the working tree to "preserve" them → RS0016-broke Data.Abstractions repeatedly, then *mis-attributed* it to Platform "live-writing" (a verify-before-claiming failure; SA corrected run→read→cite). (b) A crossed scope signal (24200 "continue W3 if capacity" vs my later 24205 "carry") led Platform to build carried W3.

## Surprises
- **moe5g9:** xUnit-v3 + Microsoft.Testing.Platform ignores `xunit.runner.json` parallel keys AND `[assembly: CollectionBehavior(DisableTestParallelization)]` via the default path — only the MTP CLI arg (`-- xUnit.MaxParallelThreads=1`) works. Non-obvious; cost several blind 4-min runs.
- **ckb35i re-classification:** the reviewer escalated an originally-"non-blocking" finding to must-fix on deeper read (a GDPR fail-open — silent PII plaintext under trimming). Good catch; I'd mis-categorized it from the first-pass label.

## Recommendations (by impact)
1. **[TRY] Design-time advertised-but-unwired gate.** A grep/analyzer over public options + capability flags asserting each has a wiring/enforcement site (or is honestly documented as not-yet). *Hypothesis:* moves the sprint's dominant defect class from REVIEW to SPEC/IMPLEMENT. *Measure:* fewer ADR-336 blockers at REVIEW next band. → self-improving candidate.
2. **[STOP] Integrator restoring carried files into the working tree.** Keep carried work in a backup/branch ONLY; restoring it into a shared project breaks the build. (My W3 RS0016 lesson.)
3. **[CONTINUE] Independent RED-first locks + clean-rebuild before trusting a lock.** Both caught real committed defects this sprint — highest-ROI practices.
4. **[CHANGE] CLOSE-gate nudge storm → server-side backoff.** Suppress phase-task re-dispatch while the owner is actively producing/running a long gate (bead `7h7srz`, 5th recurrence — client mitigation insufficient).
5. **[STOP] Crossing my own scope signals.** When I retract/supersede a ruling, reply-in-thread to the exact prior message so there's no live contradiction (the 24200→24205 W3 confusion).

## Per-Agent Praise
- **ProjectReviewer (SENTINEL):** caught 2 real BLOCKING false-safety controls + escalated the ckb35i GDPR fail-open — the review that saved the sprint's honesty.
- **TestsDeveloper:** independent RED-first locks caught 2 committed regressions; diagnosed moe5g9's MTP serial-config precisely.
- **PlatformDeveloper:** honest retractions (moe5g9 "fix didn't hold"), clean bisect, strong transport/tenancy delivery.
- **SoftwareArchitect:** run→read→cite corrections (W3 attribution, ckb35i approach, scope-real-extent) that kept the team honest.
- **BackendDeveloper:** clean settled handoffs + the misattribution catch on the RS0016 clobber.
