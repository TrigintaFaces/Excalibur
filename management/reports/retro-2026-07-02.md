# Retrospective — Sprint 864 (Advertised-Unwired Hardening + P1 Correctness)

**Date:** 2026-07-02 · **Baseline:** d4ed5ec1 → **HEAD:** 6294ffdd9 · **Facilitator:** ProjectManager (DEBRIEF)

## Commit Analysis

| Metric | Value |
|---|---|
| Commits | 55 |
| Categories | fix 25 · chore 14 · feat 4 · test 3 · refactor 3 · docs 3 · perf 1 |
| bd-tagged | 37 |
| Lines (src+tests) | +5130 / −2942 |

## Sprint Metrics

| | |
|---|---|
| Labeled `sprint-864` | 103 |
| **Closed** | **67 (65%)** |
| Carryover (open/in_prog/blocked) | 30 / 4 / 2 = 36 |
| Added mid-sprint | 0 (scope discipline held) |
| BLOCKING findings | 1 (bd-ixyew5) — resolved + committed |
| Reviews | REVIEW_CODE ✅ + REVIEW_ARCH ✅ |
| Gates | governance PASS · samples 93/93 PASS · AOT IL-clean (native-link env-waived) |

Target band was ~100; SPEC premise-gating shrank it to 67 delivered (phantom / close-as-satisfied filtering — expected, not a miss).

## What Worked Well

- **`verify-against-real-infra` paid off decisively.** The bd-ixyew5 Vault fail-open: Backend's 1-line private-method fix passed unit tests + its own reasoning, but Tests' **independent real-Vault regression lock** (non-skipped, real mid-run mount unmount) proved it RED — both public callers still re-swallowed mount-missing via `IsKeyNotFoundException`. A mocked lock would have certified the broken provider. Caught at REVIEW, before ship.
- **Clause-4 carrier kept the critical path alive.** Backend terminated mid-fix (heartbeat stale 40 min, file cold). mtime-gate confirmed dead (not a long turn) → a same-type backend-developer carrier landed the precise public-seam fix; PM still owned the commit. No stall.
- **verify-the-writer discipline (ADR-336).** PM issued ruling (A) on the re-added OrderingValidationMiddleware based on a *reader*; SA challenged with run→read→cite (zero populators). PM **retracted own ruling** → (B) revert. The advertised-but-unwired regression was reverted, not shipped.
- **Full-solution build as the integration gate** caught 5 coupled-split breaks (PayloadSizeGuard CS0122, MessageMetadata RS0016, etc.) that per-project builds miss.
- **Scope discipline:** 0 mid-sprint feature additions; discovered work tracked as carryover beads.

## What Didn't Work

- **bd daemon flush no-op at close-commit.** `bd sync --flush-only` reported success but did NOT write the just-closed ixyew5 to the jsonl; an explicit `bd export --no-auto-import` was required. The `bd-flush-then-commit` clause-6 backstop (verify committed HEAD) caught it — but only because I checked. **Recurs across sprints.**
- **AOT native-link env-fail (vswhere)** recurred again (bd-bo1axj, P2 open) — IL-clean but the gate can't self-locate `vswhere.exe`. Waived every sprint; the P2 fix keeps deferring.
- **Bare-id no-internal-refs gate is vacuous-in-reverse** (263 FP / 0 real leaks) — blocked w6hoye's DoD. SA ruled (msg 20177) to make it ref-context-qualified; carried as 78z3kq.

## Surprises

- The BLOCKING fix was **incomplete in a way the fix author + unit tests both missed** — only real infra surfaced it. Reinforces: security-path fixes need a real-infra lock, not just a unit lock.

## Test Failure Triage

- In-branch (sprint-induced): ~41 test-flips — all fixed (F-5 harness misses, NFR-6 strengthen, Caching default-on). 0 product regressions across 10 CI shards.
- Pre-existing: none surfaced net-new.
- Flaky: 1 stray testhost lock cleared during CI (not a code flake).

## Recommendations

1. **[CONTINUE]** Mandatory real-infra regression lock for any external-system/security-boundary fix. *Evidence:* ixyew5 — mocked/unit path certified a broken provider; real-Vault caught it.
2. **[CHANGE]** Harden the close-commit tracker flush: after `bd sync --flush-only`, **always verify the target bead's status in the on-disk jsonl** and fall through to `bd export --no-auto-import` if absent — mechanically, not by hand. *Evidence:* the no-op recurred this sprint; only a manual check caught it. *Effort:* small (helper in the close path).
3. **[STOP]** Deferring bd-bo1axj. The AOT native-link vswhere self-locate is a P2 that costs a manual waiver every single sprint. *Impact:* removes a recurring CLOSE-gate false-fail.
4. **[TRY]** Promote the bare-id gate precision (78z3kq) early next sprint so w6hoye's public-surface scrub can actually complete. *Hypothesis:* a ref-context-qualified matcher gives 0 FP and unblocks the DoD. *Measurement:* gate runs green on real prose, RED on a planted `bd-`/`S864` leak.

## Agent Breakdown

**Praise:**
- **TestsDeveloper** — the independent real-Vault lock is the sprint's MVP: caught a shipped-if-missed encryption-enforcement bypass, held the RED lock as the fix driver.
- **SoftwareArchitect** — challenged the PM's own ADR-336 ruling with evidence; both reviews (as ORACLE) independently re-verified BLOCKING on committed HEAD.
- **ProjectReviewer (SENTINEL)** — surfaced the Vault fail-open in a 5-agent 109-file pass; re-verified the fix run→read→cite on committed HEAD.

**Growth areas:**
- **Process (all):** the tracker-flush no-op and vswhere env-fail are *systemic* recurrences, not agent faults — fix them mechanically (recs 2, 3) so they stop taxing every close.

**Verdict:** Solid correctness sprint. One real P1 security bug caught + fixed before ship by exactly the discipline (real-infra verification) designed to catch it.
