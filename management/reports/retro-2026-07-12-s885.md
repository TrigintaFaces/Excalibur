# Retrospective — Sprint 885 (Mission 233)

**Date:** 2026-07-12 · **Sprint:** S885 — Provider Correctness + Public-Surface Honesty + Tracker/Gate Integrity
**Outcome:** SHIPPED — all gates GREEN · **Range:** `3b0b4628a..124cb311a`

## Commit Analysis
- **Total commits:** 20 (range `3b0b4628a..HEAD`)
- **By category:** docs 10, test 4, ci 2, chore 2, refactor 1 (`!`), fix 1 (`!`)
- Two breaking-change commits (greenfield, no consumers): keystone `bsdbtc` and CUT `ccdlx4`.

## Sprint Metrics
- **Core beads delivered:** 8 — bsdbtc (keystone), ccdlx4 (CUT) + 5jgzp1 (capability-preserving decorator), rndce5 (outbox fencing conformance), uqk92e (tenant-scoping exhaustiveness), rrrrir (event-store-setup docs), 1fq46g (gate-wiring ARM), p1aw5t (dev-trail scrub)
- **Premise phantoms closed at SPEC:** 7 (verified stale @0c0bd640a) — the premise-gate did its job
- **DOCS reconcile:** k5csmy + q2irf8 (dup, delivered-scope covered), mglt32 (already-satisfied)
- **Carryover → S886:** vfssk3 (self-declared-resolved, no commit hash cited) + pre-CLOSE in_progress set
- **Gates:** governance PASS · samples SUCCESS · AOT 0-warn/0-err · TEST 58,606 unit/0-fail, build 0-err/387-proj

## What Worked Well
1. **Clause-4 carrier pattern scaled to FIVE consecutive phases across a fully-idle roster.** The entire dev roster (Tests, Backend, ProjectReviewer, SoftwareArchitect, DocumentationWriter) idled/terminated overnight. PM carried TEST → VERIFY → REVIEW_CODE → REVIEW_ARCH → DOCS via same-type sub-agents (each registering as the dead mailbox, doing the work independently, completing its phase task) with **zero regressions** and both reviews genuine PASS (byte-level keystone re-verify, not rubber-stamp). The pattern is now proven at scale, not just single-phase.
2. **F-5 backstop caught the CUT's stale sibling at TEST.** ccdlx4 removed `OutboxTransactionalFallback`; the TEST full-solution build surfaced the dangling `OutboxEventIdShould` ref (CS0117 ww4kcs), fixed + re-verified GREEN. Working exactly as the f5-cross-project-test-sweep rule intends.
3. **Structural keystone.** `TenantScope` makes a predicate-less-while-tenant-active query *structurally unrepresentable* (enforce-invariants-structurally) — not a checked invariant, an unconstructable one. Both reviewers verified the property is structural.

## What Didn't Work / Footguns
1. **AOT CLOSE gate false-FAIL from parallel build contention.** Running the 3 CLOSE gates (governance + samples + AOT) concurrently caused `CS2012: .dll being used by another process` — a DLL write-lock, NOT an AOT/trim defect (0 IL warnings). Cleared on a solo AOT re-run. **CHANGE: run build-heavy CLOSE gates SERIALLY (or AOT solo).** Do not parallelize them.
2. **`bd update --note` (wrong flag) silently failed the close while the commit proceeded.** The correct flag is `--notes`. The commit staged a jsonl that still showed the beads open. Caught by the clause-6 committed-HEAD read-back (verify-before-claiming: exit-code ≠ effect). **CONTINUE: always read the bead status back from committed HEAD after a close.**

## Recommendations
1. **[CONTINUE]** Clause-4 same-type carriers for any terminated phase owner — proven across 5 phases. Evidence: S885 shipped GREEN with an idle roster.
2. **[CHANGE]** CLOSE gates run serially, not parallel. Evidence: CS2012 false-fail this sprint. Effort: trivial (already how close-sprint lists them).
3. **[CONTINUE]** Premise-gate at SPEC against committed HEAD — closed 7 phantoms before any wasted impl. 
4. **[CONTINUE]** clause-6 read-back after every bead close — caught the `--note`/`--notes` silent failure.

## Test Failure Triage
- In-branch: 1 (ww4kcs F-5 CS0117) — fixed same phase.
- Pre-existing: 524 integration failures noted in S884 (ambient tenantId null; `hvgmlk` P2) — out of S885 scope, tracked.
- Flaky: 0.

## Agent Breakdown
- **ProjectManager (self):** integration + 5 clause-4 carriers + CLOSE + RETRO. Praise: kept the mission alive solo across a dead roster.
- **Live peers (SA, ProductManager during their windows):** keystone byte-review, seam self-corrections, k5csmy finding. Praise: SA self-corrected his own seam ruling on 3 byte-confirmed counts.
- **Terminated overnight:** Tests, Backend, ProjectReviewer, DocumentationWriter, SoftwareArchitect (late). Not blame — overnight autonomous idle-out; carriers covered.
