# Retrospective — Sprint 876

**Provider-Expansion W1 (Oracle + Pulsar) + Dashboard Hardening + Decider/Cascading**
**Date:** 2026-07-06 · **PM:** ProjectManager · **Impl HEAD:** `821096067` · **Closeout:** `f63c9fa`

---

## Commit Analysis

| Metric | Value |
|--------|-------|
| Period | 2026-07-05 23:53 → 2026-07-06 08:28 (~8.5h) |
| Total s876 commits | 14 |
| Lines added / removed (closeout+recent) | ~1,051 / ~17 |

**By category:** plan 1 · feat 5 · fix 3 · test 3 · docs 1 · chore 1.

feat: Oracle provider W1 (EventStore/Outbox/Inbox/Saga, Dapper), Pulsar transport W1 primitive,
C1 aggregate-handler Decider, C2 cascading messages, dashboard `/saga/stuck` clamp + read-auth.
fix: Oracle outbox reserve zero-row delivery-loss, Pulsar PublicAPI baselines, dashboard hardening.
test: Pulsar real-infra conformance lock, C1 author≠impl augmentation (4/4), SPA Vitest/Svelte (30/30).

## Sprint Metrics

| | |
|--|--|
| Beads delivered | ~23 (Oracle A1–A5, Pulsar A7–A8, Decider C1, Cascading C2, dashboard cluster, 2 review findings) |
| Carried over | 6 non-blocking REVIEW beads → next band |
| Added mid-sprint | 2 (review findings ir6hbu/867wqw — blocking bugs, in-scope) |
| Blocked | 0 |

**Gates:** CI 109,586 pass / 0 real regressions · Governance 372 proj 0 err · Samples 92/92 ·
AOT 0 IL-warn · REVIEW_CODE + REVIEW_ARCH PASS 0-blocking.

## Pattern Analysis

**What worked well**
- **Layered review caught a CI-invisible blocking bug.** REVIEW_ARCH's 5-way specialist + source-reasoning
  found the Oracle outbox reserve zero-row silent stall (ODP.NET positional binding: shared 3-param
  `DynamicParameters` → `@DispatcherId` bound to `Parameters[0]=BatchSize` → 0 rows every reserve =
  delivery-loss). REVIEW_CODE **and** full CI missed it because the B2 conformance lock was **vacuous**
  (all asserts pass on empty sets). Reinforces `verify-against-real-infra-not-mock` + non-vacuous-lock bar.
- **`speak-up-challenge` live twice.** SA self-retracted the A8 Pulsar thin-adapter approval after Platform's
  grep-evidence (all 5 transports back `SendAsync` with a full `*MessageBus` → thin adapter precedent-less);
  W1 shipped honest DI-resolvable primitives only, full adapter deferred to W2 (`ro8zw3`). SA also skipped the
  zero-impl `ITransportProvider` dead abstraction to avoid an ADR-336 orphan.
- **Carrier sub-agent** covered the terminated DOCS session; docs-site GREEN, internal-ref scan clean.

**What didn't work**
- **CLOSE-gate server auto-nudge storm — 4th recurrence (S867/S874/S875/S876), and the S867 mitigation
  proved insufficient.** Task #2479 was re-dispatched 5× (msgs 23810/23811/23814/23821/23822) during the
  long `validate-samples.ps1` build **despite** posting the `in_progress` heartbeat the S867 learning
  prescribed. Each nudge fires the Stop hook and burns a turn/tokens. The heartbeat-before-gates mitigation
  does not stop a cadence-based server re-dispatch. → `/self-improving` candidate: **server-side nudge-backoff
  for a phase owner whose task is `in_progress` and whose session is actively producing** (or exempt
  gate-running phase owners), not just a client heartbeat.

**Surprises**
- **Pulsar PublicAPI dormancy trap:** a new shipping package with NO `PublicAPI.{Shipped,Unshipped}.txt`
  leaves the RS0016 analyzer **dormant** → package builds 0/0 in a per-lane build, hiding the gap; only the
  `PublicApiContractShould` contract test catches it at full-CI. Package-scaffold checklist should require
  both files at ADD time.

## Test Failure Triage

| Class | N | Resolved? |
|-------|---|-----------|
| In-branch (sprint work) | 2 (Pulsar shard-1 PublicAPI, Oracle outbox B1/B2/B3) | YES — fixed + re-verified |
| Pre-existing | 1 (`ExactlyOnceMeterCollection` flaky, passes isolated) | Tracked `bd-r0oidw` |
| Flaky | 0 new | — |

## Recommendations

1. **[CONTINUE] Layered REVIEW_ARCH source-reasoning + non-vacuous real-infra locks.** Evidence: caught the
   Oracle delivery-loss bug that 109k CI tests + REVIEW_CODE missed (vacuous lock on empty sets).
2. **[CHANGE] Escalate the CLOSE-gate nudge storm to a structural fix (`/self-improving`).** Evidence: 4th
   recurrence; the client-side heartbeat (S867 rec) did NOT stop it this sprint. Server should back off
   re-dispatching an `in_progress` phase task whose owner session is active/producing.
3. **[TRY] Package-scaffold gate requiring `PublicAPI.{Shipped,Unshipped}.txt` at project-ADD.** Hypothesis:
   moves the RS0016-dormant trap from full-CI (late) to ADD-time. Measurement: no shard-1 PublicAPI RED on
   the next new shipping package.
4. **[CONTINUE] Run long gates as backgrounded tasks + block-wait to consolidate Stop-hook churn.** Evidence:
   block-waiting the samples gate collapsed repeated nudge turns into one.

## Agent Breakdown (praise)
- **SoftwareArchitect:** caught the blocking Oracle bug via source-reasoning; two grounded self-retractions
  (A8 adapter, `ITransportProvider`) — highest-leverage work of the sprint.
- **PlatformDeveloper:** grep-evidence that reshaped the Pulsar seam; found the PublicAPI dormancy trap.
- **TestsDeveloper:** honest VERIFY self-correction; non-vacuous `union.Count==10` lock re-authored.
- **ProjectReviewer / DocumentationWriter:** clean REVIEW_CODE + carrier-covered DOCS, internal-ref clean.
