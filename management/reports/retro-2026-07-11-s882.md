# Retrospective — Sprint 882 (DEBRIEF)

**Date:** 2026-07-11 · **Mission 230** · **Range:** `799786b0c..edb5df324` · **Verdict:** SHIPPED (product) / instrument-wave waived

---

## Commit Analysis

```
Period:        2026-07-10 → 2026-07-11 (S882)
Total commits: 61
Files changed: 268
Lines added:   15,364
Lines removed: 5,416

By category (subject prefix):
  plan:    19 (31%)   ← PLAN-phase amendment storm (see "What didn't work")
  fix:     15 (25%)   ← the 30-P0 product substance
  chore:    9 (15%)   ← tracker-corruption recovery
  docs:     7 (11%)
  test:     4 ( 7%)
  feat:     2 ( 3%)   ← materialized-view delivery semantics; outbox capability seam
  revert:   2 ( 3%)   ← bead-nudge regression; tz2fks keystone
  cut/merge/spec: 3 ( 5%)
```

Integration sub-set I drove this session (post-restart): **9 commits `b39f3d97d..edb5df324`**, each explicit-staged, `.beads/` protected.

## Sprint Metrics

```
Planned P0s:        30 (premise-verified ALIVE before scoping; 10 of 40 open P0s triaged out as phantom)
Product P0s landed: 30 — all traced to GREEN locks (VERIFY waves B/C/D/E/F1)
REVIEW blockers:     3 (B1 markers, B2 fencing, B3 inbox+projection) — all fixed + locked + re-verified
Carried over:        Wave-A instruments (f5-sweep wiring FR-A3/1fq46g) + ~9 in_progress + 3 ARCH follow-ups
Gates:               REVIEW_CODE PASS · REVIEW_ARCH PASS (0 blocking) · governance 387/387 · samples 94/94 · AOT 0-warn
```

## What Worked Well

- **Layered independent review caught 5 real defects unit gates missed.** Full-CI backstop found #1 (tenancy-marker inert-requirement gap) + #2 (l0qpxo consumer-mock); REVIEW_CODE found 3 blockers on committed HEAD; REVIEW_ARCH verified every ruled invariant *structurally* (read impl, not just tests). None reached shipped product.
- **`issue-remediation-protocol` ran textbook on #1.** Finder (Tests) posted evidence and did NOT fix; PM triaged 3 disjoint lanes; SA ruled scope; single-actor each → **zero collisions**. The `T` false-positive was correctly split from the two real gaps.
- **author≠impl regression locks, RED-first.** B2/B3 locks proven RED on the pre-fix HEAD, GREEN on the fix — non-vacuous. Tests self-caught a phantom drain-arm finding via `verify-before-claiming`.
- **Integrator discipline held under a corrupt tracker.** Explicit per-lane staging, never `git add -A` — which is exactly what kept the daemon-clobbered `comments.jsonl` out of every commit. Under-stage check (zero uncommitted `.cs`) proved committed HEAD == verified tree.
- **The wake-signal convention worked in practice** — every `critical`+`ackRequired` directed message woke its idle recipient within a poll cycle; the mission never stalled on unread FYI.

## What Didn't Work

- **The tracker tooling is now eating sprints — this is the #1 recurring cost.** `log-discovery.sh` reported success and wrote nothing (destroyed ~45 CEO findings, `mhbin6`); `bd export` drops all comments (`e8okhg`) and descriptions (`tbntzy`); the daemon truncates `issues.jsonl` (`dcmvsh`) and clobbers `comments.jsonl` (`2t6lfx`, a 12.5k-line churn this sprint); a prior integrator shipped a fix but omitted `issues.jsonl` (`uebl66`). Nine `chore(tracker)` commits this sprint were pure recovery.
- **PLAN was a ~30-amendment storm.** 19 `plan:` commits, many self-correcting/retracting ("A-27 my brick was wrong", "A-13 my Wave C unification was wrong", "A-11 my fence premise was false"). Premise-verification happened *as in-flight amendments* instead of before the scoping commit.
- **The integrator (prior PM session) went offline mid-CLOSE and stalled the whole team ~1h.** Five agents + the reviewer sat correctly idle (single-integrator invariant) until the operator manually restarted the PM window. `verify-worker-liveness-before-dispatch` exists, but the machinery still routes a phase into a frozen session.
- **The sprint's own declared HARD GATE never got built.** f5-sweep wiring (FR-A3/`1fq46g`) — "the MANDATORY gate that has never run" per the mission title — remained unwired; waived to carryover.
- **Environment breakage cost real cycles.** Bash-tool received a Windows-format `$PATH` → `git`/`python`/`bd` unresolved (all integration re-routed through PowerShell); a **39-hour-old zombie test-host** (PID 39452, from 2026-07-09) held DLL locks and failed the first clean build.
- **The bead-nudge regression (`3679a2883`) had to be reverted mid-sprint** — the per-bead wake that told idle agents to implement any assigned bead in any phase, the root of the "8 agents on one issue" meltdown.

## Surprises

- TestsDeveloper asserted "no Docker" for 4+ turns without ever running `docker info` — a `verify-before-claiming` miss about its *own* tooling, self-retracted when TEST forced the check (Docker was up the whole time; the real-infra isolation lock then passed 1/1).
- The projection query-helper (`BuildTenantScopedWhereClause`) fail-closed decision surfaced mid-integration and needed a fresh SA confirm — a good catch (Backend refused to silently ship either a leak or an over-harden).

## Recommendations (ordered by impact)

1. **[CHANGE] Fix the tracker tooling — it is now the dominant recurring cost.** The corruption cluster (`mhbin6`, `e8okhg`, `tbntzy`, `dcmvsh`, `2t6lfx`, `1iz27o`, `uebl66`) turns every sprint's bookkeeping into a data-loss minefield. *Evidence:* 9 recovery commits + ~45 destroyed findings this sprint alone. *Effort:* medium — fix `log-discovery.sh` silent-write-failure first (highest leverage), then the daemon truncation/clobber.
2. **[CONTINUE] Layered independent review + author≠impl RED-first locks.** *Evidence:* 5 real defects caught across full-CI / REVIEW_CODE / REVIEW_ARCH, zero reached product; the structural REVIEW_ARCH pass verified invariants tests alone couldn't.
3. **[STOP] In-flight PLAN amendment storms.** Verify premises against committed mainline *before* the scoping commit, not as 30 self-retracting amendments. *Impact:* a readable plan history and less mid-sprint premise churn (composes with `scope-the-keystone-to-its-real-extent`).
4. **[TRY] Server-side worker-liveness-on-dispatch.** A mission phase should not auto-dispatch into a frozen session. *Hypothesis:* eliminates the recurring "integrator offline mid-phase → whole team stalls until manual restart." *Measurement:* zero manual-restart stalls next sprint.
5. **[CHANGE] Session-start environment hygiene.** Sweep orphaned test-host processes and validate the Bash-tool `$PATH` is POSIX at session start. *Evidence:* a 39h zombie broke the first build; the Bash PATH broke every `.sh` hook/gate from the interactive tool.

## Carryover / follow-ups filed

- **Wave-A instruments** (waived): `1fq46g`, `fba6rc`/`ud36ow`, `dmo9bk`, `mxy5rv`, `2t6lfx`, `svacnv`, `exo4ka`, `rndce5`, `uqk92e`, `bsdbtc`, `ihekz2`.
- **ARCH non-blocking:** `nb1qxp` [P1] saga DTO fail-open sweep; `s25n17` [P2] SOC2 secondary-provider PASS (→ ProductManager); `vbki0n` [P3] TenantOwnedContracts oracle completeness.
- **New infra beads to file:** Bash-tool Windows-`$PATH`; stale-test-host zombie sweep; `log-discovery.sh` silent-write-failure (`mhbin6` — highest leverage).
