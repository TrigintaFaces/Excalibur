# Retrospective — Sprint 862 (2026-07-01)

**S862 — Reliability Seams, Provider Validation & Review-Debt Burn-down**

## Commit Analysis
- **38 commits** · 146 files · **+3,616 / −419** · 23 distinct beads.
- By category: fix 17 (45%) · test 8 (21%) · chore 5 · feat 4 · docs 4.
- Fix-heavy profile is expected for a reliability/review-debt sprint.

## Sprint Metrics
- **15 IMPLEMENT beads** delivered + operator flaky-determinism directive (5/5) + 4 REVIEW_CODE fixes (B1/B2/B3/P1) + 887vwl hardening.
- **0 regressions** in CRUCIBLE full-CI (~119,669 passed / 48 skipped, clean 10-shard).
- VERIFY: full coverage (13/13 code-beads → passing, mostly RED-proven).
- Both reviews APPROVED (REVIEW_CODE + REVIEW_ARCH on 3287e2133); governance-stack PASSED (360/360).
- P0 (B1/B2 security): 2/2 fixed. P1: 45jbjv, 0a7kvu, 887vwl fixed.

## What Worked Well
- **Grounded stale-premise challenges repeatedly prevented shipping non-work.** pihoca (OutboxMessage is a distinct subsystem, not a dup — avoided a risky rewrite), beke9c (already-satisfied), dhsik1 3-fix coverage (real honoring types via a `Removed` arm, not fabricated). Each caught by run→read→cite before code.
- **Layered review caught real defects the full-CI missed.** REVIEW_CODE surfaced 3 BLOCKING bugs on our own S862 work (RabbitMQ poison-loop, raw-PII-default A02, standalone DI gap) that ~119.7k green tests didn't — targeted review > coverage for behavior/security seams.
- **Flaky-determinism diagnosis:** 3 of 5 "flaky" tests were deterministic bugs in disguise (stale-DLL, false exit-0 zero-match filter, a real Security DI product bug). Root-causing beat quarantining.
- **Carrier sub-agent kept the critical path alive** when Backend went offline mid-review — B2/P1 impl delivered, PM-gated, author≠impl preserved.

## What Didn't Work
- **PM ownership thrash (m084l4, dhsik1, pihoca).** I re-pinned ownership without first checking ground truth, causing repeated flip-flops workers had to STOP-and-coordinate around. Fix: verify tree/whois BEFORE issuing a pin; "the work that exists wins."
- **F-5 cross-project narrowness recurred** (wrht38 SqlServer sibling in a different test project; ufbn29 FullJitter enum siblings) — caught only by CRUCIBLE's full-solution build, not the per-bead sweep.
- **Integrator index hygiene:** an l55sbl commit swept a worker's pre-staged `git rm` (guard-move WIP) → briefly broke committed HEAD. Fix: `git reset` the shared index before staging each bead.
- **Stranded post-integration deltas** (SA's ADR Amendment 5, Tests' coverage) needed catching before REVIEW — additive deltas must commit before the reviewer runs.

## Recommendations
1. **[CONTINUE] Grounded challenge before building** (run→read→cite; STOP-and-ask on a stale/ambiguous premise). Evidence: pihoca/beke9c/dhsik1 each avoided a non-fix or risky rewrite.
2. **[CHANGE] PM verifies ground truth (tree/`whois`) BEFORE issuing an ownership pin.** Evidence: 3 ownership thrashes cost coordination cycles; every one resolved by ratifying what was already built.
3. **[CHANGE] Run `eng/ci/f5-sweep.sh` pre-REVIEW, not as the CRUCIBLE backstop.** Evidence: 2 F-5 recurrences (wrht38, ufbn29) this sprint.
4. **[TRY] Integrator `git reset` the shared index before staging each bead.** Hypothesis: eliminates the pre-staged-foreign-change class (the l55sbl HEAD-break). Measurement: zero cross-lane sweep incidents next sprint.
5. **[CONTINUE] Carrier sub-agent for a terminated worker on the critical path** (verify-worker-liveness cl.4) — kept REVIEW_CODE moving with author≠impl intact.

## Carryover / Risk
- Real-infra-blocked (Cosmos emulator dead / real Kafka): wxw6c3, g7k4m3, 5e60ry, las5x6, aqfkr6 — provider-testing epic needs a working emulator env (S863).
- osj47o (sanitizer unsalted-hash → salt/erase redesign), 4h18pa (CHANGELOG footer scrub), vfssk3 (manual triage).
