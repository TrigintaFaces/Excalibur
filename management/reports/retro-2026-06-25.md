# Retrospective — Sprint 849 (Messaging Reliability Hardening)

**Date:** 2026-06-25 · **Author:** ProjectManager (DEBRIEF) · **Verdict:** GREEN

## Commit Analysis
- **Period:** S849 (plan `53ec2ba5f` → closeout `1cb23744b`)
- **Total:** 22 commits · **+3510 / −171** · 65 files
- **By category:** fix 4 · test 3 · docs 5 · ci 2 · chore 8 (close/closeout)
- **Shape:** every code `fix:` is paired with an author≠impl `test:` lock and a `chore:` bead-close — the integrate-and-prove cadence held across the sprint.

## Sprint Metrics
- **Planned lanes:** 8 (K `rb4g4b` keystone · R1 `maqsxi` · R2 `o045q4` · R3 `gejhft`+`ffxglb` · R4 `g0lodv` · I1 `xlnd5e` · F1 `kc148w` · doc `bahmnc`) — **8/8 completed**.
- **Added mid-sprint:** `txmwh9` (P1 regression caught in TEST) + 2 review findings (`h41806` doc, `apgcp9` spec-AC pins) — all closed.
- **Carried → S850:** `aed1gl`/`q29qfg`/`14z4ao` (sanctioned R3 deferrals) · `gec369`/`lpnfln` (pre-existing IntegrationTests reds) · `cy0hgp` (sample README DDL) · `pkj4sz`/`ouwbr1`/`23cxgz` (AOT toolchain env).
- **Blocked:** 0.

## Test Failure Triage
- **In-branch (caused by sprint work):** 1 — `txmwh9` (K's unconditional `Replace` clobbered consumer registry). **Fixed** before REVIEW (`dc59f80d7` + 4 author≠impl locks).
- **Pre-existing:** 21 IntegrationTests (`gec369` 18× snapshot-DDL drift, `lpnfln` 3× Elastic/AzureSB) — **Beads created, deferred S850**. Independently git-verified non-S849.
- **Flaky:** 0 (the IntegrationTests reds were initially *suspected* flaky under concurrent load; clean-isolation re-run proved deterministic).
- **Fixed (bonus):** 0.

## What Worked Well
- **Diagnose-open / fix-by-assignment on `txmwh9`.** Finder (Tests) filed + evidenced; PM assigned single-actor lanes (SA seam → Backend impl → Tests independent locks → PM integrate). Zero file collisions; the independent lock seat caught the instance-vs-type/factory discriminator gap.
- **Disagree-and-commit, evidence over authority.** SA recommended ADOPT; the team refuted with code-verified evidence (`DispatchCacheManager:55` concrete-cast hazard, configure-time asymmetry, ctor-timing); SA *reversed his own call* to guarded-`Replace`. Best answer won, fast.
- **Pre-CLOSE AC-drift catch (`apgcp9`).** Holding the uncommitted spec pins surfaced that the CB-open hard AC (15395) and decorated-path AC (15385) — both ruled *during* IMPLEMENT/TEST — were never written into the spec. The committed AC ground-truth would otherwise have shipped not matching the locks.
- **commit-surface-before-parallel-edits.** Committing Backend's gated `DispatchBuilder.cs` the instant he signalled ready let Tests RED-prove against a clean committed parent with zero risk to the uncommitted fix.
- **Non-vacuous author≠impl locks throughout** (RED-proven on the true pre-fix parent in worktrees, never a whole-file `git checkout`).

## What Didn't Work / Surprises
- **K's integration gate missed `Excalibur.Dispatch.Tests`.** K ran Configuration + Messaging suites but not the consumer-DI idempotence shard where `txmwh9` lived → the regression slipped to the TEST-phase full-CI backstop. The backstop worked, but late.
- **`gec369` is *another* F-5 cross-project miss** (test-DDL not updated when prior `2b615bc8c` added the `Metadata` column). The cross-project test-sweep rule exists; the *test-DDL* dimension keeps recurring.
- **bd-flush daemon contention persisted** — `bd sync --flush-only` silently no-op'd / "Access is denied" on rename; resolved each time via `bd export --no-auto-import` (cl.6). Recurring tax, now reflex.
- **AOT native-publish toolchain gap** (`vswhere`/`link.exe` not on PATH) recurred — and is **triple-tracked** (`pkj4sz`/`ouwbr1`/`23cxgz`), itself a dedup-at-creation miss.
- **Closeout pre-commit hook auto-staged foreign files** (`poll-opcom.sh`/`settings.json`/`validation-results`) beyond the explicit `git add` set — operator accepted, but the integrator's "stage only in-scope" intent was silently widened by a hook.

## Recommendations (by impact)
1. **[CHANGE] Keystone/DI-contract changes must gate `Excalibur.Dispatch.Tests` (consumer-DI idempotence shard), not just Configuration+Messaging.** Evidence: `txmwh9` slipped K's gate and surfaced only at TEST full-CI. Effort: one line in the keystone gate checklist.
2. **[CHANGE] Add a *test-DDL* dimension to the F-5 cross-project sweep** — on any schema/column change, grep `tests/**` integration DDL for the affected table. Evidence: `gec369` (recurring class). Effort: extend the existing sweep checklist.
3. **[TRY] Mechanical pre-CLOSE "spec-AC ⇄ locks" grep.** Hypothesis: rulings made during IMPLEMENT/TEST routinely don't land in the spec; a grep of each lock's named AC against the spec catches drift. Measurement: zero AC-drift findings at REVIEW/CLOSE next sprint. (`apgcp9` was caught by diligence, not process.)
3. **[CONTINUE] diagnose-open/fix-by-assignment + independent author≠impl locks + disagree-and-commit.** Evidence: the entire `txmwh9` remediation — clean, collision-free, and the independent seat caught a real gap.
5. **[STOP/FIX] Dedup the 3 AOT-toolchain beads and fix the env once** (Developer-Command-Prompt / vcvars on the close agent) so the AOT gate stops being waived. Impact: removes a recurring CLOSE waiver.

## Agent Breakdown (praise-first)
- **TestsDeveloper** — caught `txmwh9` in TEST full-CI, authored every non-vacuous lock independently, and rigorously characterized the 21 pre-existing reds (isolation + git-range proof). Standout sprint.
- **SoftwareArchitect** — modeled disagree-and-commit by reversing his own ADOPT recommendation on the team's evidence; thorough 3-lens REVIEW_ARCH.
- **BackendDeveloper** — decisive grounded findings (cast hazard, ctor-timing) that shaped the seam; clean single-actor impl; self-corrected the F-5 compile-vs-test signal.
- **ProductManager** — the `apgcp9` AC-drift catch at the pre-CLOSE boundary; crisp AC rulings (CB-open hard AC, ownership-transfer).
- **ProjectReviewer** — independent committed-HEAD verification (never trusting prior reviews); surfaced the instance-vs-type/factory lock-completeness gap.
- **DocumentationWriter** — corrected stale outbox DDL in docs + flagged the sample README DDL mismatch (`cy0hgp`).

**Growth area (team-wide, not blame):** integration *gate scoping* — both `txmwh9` (K gate too narrow) and `gec369` (test-DDL sweep gap) are "the guard didn't cover the boundary it should have." Recommendations 1–2 target exactly this.
