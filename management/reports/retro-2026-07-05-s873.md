# Retrospective — Sprint 873 (Remediation & Wire-Completion)

**Date:** 2026-07-05 · **Mission:** 221 · **Final HEAD:** `63662ce23` (baseline `f28faacc4`) · **Status:** CLOSED, all phases green, both reviews PASS

## Commit Analysis
- **23 commits** · 116 files · **+2,060 / −5,524** (net −3,464 — remediation/cut-heavy, as scoped)
- By category: fix 7, refactor 4, chore 4, feat 3, docs 3, test 2

## Sprint Metrics
- **18 lane beads** (17 s873 + `4qtxn5` S868 bonus) — **all delivered + closed**; `maf76z` carved to next sprint (SA feature-sized ruling). 100% accounted.
- New follow-ups filed: `f6mr3g` P3 (base64url→`Base64Url` cleanup, pre-existing), `gnjip4` P2 (`Excalibur.Dispatch.ExactlyOnce` meter not in `AllMeterNames` — advertised-but-unwired, DOCS-carrier catch).
- Gates: TEST 10/10 shards (Excalibur 23,419/0 · Messaging 6768/0 · Transport 4543/0+533/0 · Middleware 2032/0 · Conformance 168/0 · Patterns 660/0); VERIFY 16 COVERED/0 GAP; SAMPLES 92/92; REVIEW_CODE PASS (1 MINOR); REVIEW_ARCH CLEAR (6 seams non-vacuous); governance-stack PASS. **AOT re-gate waived** (env deadlock, no AOT-relevant delta, S872 AOT-clean).

## Test Failure Triage
- **TEST gate caught a 4-cluster remediation** (all fixed + re-verified GREEN): A hl7tzr DLQ conformance (flip-to-4 + AwsSqs native redrive), B ma1jc7 saga real-DI regression (mock-grade WIRE lock hid a broken DI registration → `IServiceProvider` ctor + strengthened real-DI-resolve lock), C caching CoverageBoost stale F-5 siblings, D xh4jru retry-reflection F-5.
- **Env:** recurring zombie-msbuild/dotnet CI deadlock (≥34 procs, 0 output) at the CLOSE AOT re-gate — same class as S872. Cleared by kill.

## What Worked Well
- **The gate caught EVERY integration under-stage on committed HEAD, never in production.** ~9 instances this sprint (hl7tzr orphan tests + ADR-113 correction, xh4jru/6v59o0/63elhi wiring completions, DLQ-conformance, caching, retry, saga-hosted-svc). Each surfaced at the committed-HEAD build or the 10-shard CI or a reviewer's committed-HEAD re-verify — exactly where the gates are designed to catch them.
- **Clause-4 carriers covered TWO terminated overnight sessions** (ProjectReviewer + DocumentationWriter) with genuine independent run→read→cite work — the REVIEW_CODE carrier verified builds/rulings and confirmed the serializer guard 9/9; the DOCS carrier found real drift across 8 docs and filed a real follow-up (`gnjip4`). Pipeline never stalled with the operator unavailable.
- **Owner-ruled seams with evidence-driven corrections on every side** — SA self-corrected the projection-serializer canonical + the 6v59o0 bus-layer assumption; PM retracted an allowlist misread in favor of SA's converge-all; Backend's grounded challenge upheld; Platform's `AddSagas()`-without-store design catch turned a registration fix into a genuine improvement.
- **Non-vacuous structural guard** (`EventStoreCanonicalSerializerGuardTests` 9/9) caught the missed SqlServer projection store (target-typed `new()` the literal-grep survey missed) at the integration gate.

## What Didn't Work / Recurring
- **Integration under-staging + too-narrow F-5 sweep (~9× this sprint) — the headline.** Per-lane `git add <subset>` repeatedly missed wiring files, F-5 sibling tests across non-obvious projects (Middleware.Tests, CrossTransport, Conformance, Saga.Tests, Dispatch.Tests), orphan test directories, and a stranded ADR. Root cause: `--no-incremental` builds the working tree (which had the files), so local builds passed while committed HEAD was a subset. The gates caught all of it, but it cost TEST-phase + REVIEW-phase cycles.
- **Mock-grade WIRE lock** (ma1jc7) passed 2/2 on test-provided deps while real DI was broken — SA diagnosed.
- **Env instability:** zombie-msbuild CI deadlock recurred (S872-class).

## Recommendations
1. **[CHANGE — PROMOTE]** Build the COMMITTED SHA + stage the FULL lane diff + F-5-sweep ALL sibling projects. Evidence: ~9× recurrence in one sprint. → clauses on `clean-rebuild-before-trusting-locks.md`, `forge-integration-conventions.md`, `f5-cross-project-test-sweep.md`.
2. **[CHANGE — PROMOTE]** A WIRE lock must resolve the consumer through REAL DI, not test-provided deps. Evidence: ma1jc7. → clause on `verify-against-real-infra-not-mock.md`.
3. **[CONTINUE]** Clause-4 carriers for terminated phase owners. Evidence: 2 clean coverages (review + docs).
4. **[CONTINUE]** Non-vacuous structural guards + verify-before-claiming (build+RUN the lock). Evidence: caught missed SqlServer store + c4anmz non-compiling lock.
5. **[TRY]** Periodic inter-agent `dotnet`/`msbuild` process cleanup to prevent the recurring CI deadlock.

## Rule-Promotion (to /self-improving)
- PROMOTE: under-stage/committed-SHA/full-lane-diff/cross-project-F-5 clauses (≥3× bar far exceeded — ~9×).
- PROMOTE: mock-grade-WIRE-lock → real-DI-resolve clause.
- ASSESS (likely track, not mint): enumerate-by-real-indicator, build+RUN-lock — probably already covered by verify-before-claiming/clean-rebuild.
- DECLINE new files for CONTINUE items (clause-4-carrier, structural-guard, verify-before-claiming all worked as written).
