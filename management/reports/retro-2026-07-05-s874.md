# Retrospective — Sprint 874 (Saga Lifecycle + maf76z Keystone)

**Date:** 2026-07-05 · **Final HEAD:** ea55fd870 · **Verdict:** COMPLETE, clean, 1 tracked P1 carryover.

## Summary
Cross-provider transactional inbox keystone (maf76z, Mongo+Cosmos) + saga retention purge ×3 shipped.
Full pipeline PLAN→…→RETRO ran auto-advancing. Headline event: **REVIEW_CODE caught a real exactly-once
correctness defect that 118,609 passing CI tests missed**, remediated in-sprint without a re-plan.

## What Worked Well
- **Layered review caught what CI could not.** ProjectReviewer found B1 (Cosmos `UpsertItem` non-atomic
  claim → concurrent double-execute) by source-reasoning on committed HEAD. The full 10-shard CI was GREEN
  because the Cosmos integration path was **emulator-unreachable** (HttpClientFactory never wired) — the lock
  literally could not run. Real-infra fidelity > green count.
- **issue-remediation-protocol ran textbook.** Diagnose-open (Reviewer+SA+Backend+Platform all analyzed) →
  PM assigned ONE fixer (Backend) + independent lock author (Tests) → coupled integration → re-verify. Zero
  parallel-edit collisions.
- **Grounded IMPLEMENT challenge surfaced the missing dependency.** Tests, before authoring, found the
  `HttpClientFactory` omission (every sibling Cosmos store wires it; inbox alone didn't) — the exact reason
  B1 shipped untested. Sanctioned as a missing-dep on B1's lane, not scope creep.
- **Altitude-check on the emulator block.** Reviewer ruled graceful-degrade (env-parity) with 3 hard
  conditions + kept bd-etm9ih OPEN as must-run-before-CLOSE — deferred+tracked, not waived. Neither
  band-aid nor rabbit-hole (didn't block the sprint overnight on an emulator that won't boot here).
- **Clean gates:** REVIEW_CODE PASS, REVIEW_ARCH CLEAR/PASS 0-blocking, governance/samples(92-92)/AOT(0-warn) all GREEN.

## What Didn't Work
- **CLOSE-gate auto-nudge storm recurred (2nd occurrence, after S867).** Task #2455 re-dispatched ~5× during
  the multi-minute governance/samples/AOT gate runs despite an in_progress heartbeat + task-progress updates.
  Wasted turns acking stale nudges. Not yet ≥3× for a rule — tracked for recurrence.
- **bd daemon contention** — 5 duplicate `bdservicehost.exe` procs caused desync; worked around with
  `--no-daemon` + safe `bd export --no-auto-import`. Needs an operator restart (escalated).

## Surprises
- A 118k-passing-test suite gave zero signal on a data-loss-class exactly-once defect — because the test that
  would catch it was structurally unable to execute (unreachable infra). "Green" was silent about an entire
  provider path.

## Test Failure Triage
- In-branch defects (B1 exactly-once): 1 — fixed + coupled regression lock landed.
- Pre-existing/unrelated: 2 — bd-moe5g9 (Transport teardown, P1), bd-r0oidw (AsyncRisk meter flake, P2), tracked.
- Flaky: 0 new.

## Recommendations
1. **[CONTINUE]** Layered source-reasoning review on committed HEAD after CI-green. Evidence: it caught a
   data-loss defect CI couldn't.
2. **[CONTINUE]** Deferred+tracked (never waived) for genuinely infra-gated proofs — bd-etm9ih pattern.
3. **[CHANGE/TRACK]** CLOSE-gate nudge storm — 2nd occurrence. If it recurs a 3rd time, promote a rule/hook
   fix (post an in_progress phase-heartbeat before long gates, or raise the server nudge backoff for phase
   owners actively running gates).
4. **[TRY]** Run heavy CLOSE gates (samples/AOT) as background tasks up front in parallel where safe, to
   shorten the nudge-exposed window.

## Carryover
bd-etm9ih (P1, empirical Cosmos exactly-once on a healthy emulator) · 6icbfd CAS (infra-gated) ·
bd-moe5g9/bd-r0oidw (pre-existing) · ISagaStore default-exception-message discovery.
