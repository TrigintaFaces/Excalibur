# S865 Retrospective — MS-First / BCL-Reinvention Remediation

**Date:** 2026-07-02 · **Callsign:** DEBRIEF · Sprint closed @`7a86421f`

## Commit Analysis
- **35 commits** (7ad5c5be..HEAD): fix 14, refactor 9, test 4, chore 4, docs 3, build 1
- **Churn:** +4595 / −5168 (**net −573** — delete-and-delegate removals outweigh additions, as intended)
- **126 files** touched

## Sprint Metrics
- **Delivered: 30 beads** (core WWMD reinvention theme **100%**); **carried: 33** to S866 (worker-triaged: Lane B/D hygiene, SPIKEs, keystones h21yy7/xng6mv, held xs1qfn)
- **P1:** 2/2 crypto delivered (fxn2z3 CSPRNG, hnx8gw quorum fail-closed). **BLOCKING reinventions:** 5/5. **P2/P3 §2b + deletes:** delivered.
- **Mid-sprint discovered work:** ~8 §2b bugs + 2 P1 crypto (from the audit) + 5 TEST-phase regressions — all resolved, none deferred silently.

## Quality / Gates
- Full-CI **9/10** shards GREEN (bd-jetk8q serial-config test-infra tracked, not a regression). VERIFY **100% MUST coverage**. SAMPLES 0 fixes.
- REVIEW_CODE PASS, REVIEW_ARCH APPROVED (0 blockers). Governance PASS (360 proj), Samples 93/93, AOT 0 IL warnings.
- Real-infra: hnx8gw 3/3 on SqlServer.

## Test Failure Triage
- In-branch (sprint-caused): 5 CRUCIBLE regressions (bd-70gvqt DynamoDb ×3, bd-qun6gg ContentHash ×2) + 1 P1 valid-recovery (bd-0ygm57) — **all fixed + re-verified**.
- Pre-existing: bd-jetk8q (Transport serial-config deadlock) — Beads task created (P2). 2 discovery keep-open (no commit cited).
- Flaky: none new.

## What Worked
- **Layered adversarial verification** is the headline: real-infra lock (hnx8gw) → CRUCIBLE full-CI (5 regressions) → SA source-read (vacuous-lock catch) each caught what the prior missed. A P1 recovery bug, a vacuous lock, and 5 CI regressions were all caught **before ship**, not asserted away.
- **Delete-and-delegate** validated: zero-consumer deletes → 0 sample fixes, clean PublicAPI removal.
- **Coupled non-vacuous locks** (author≠impl) held the §2 bar — every keystone/§2b/crypto had a RED-on-pre-fix lock.
- **Grounded scope challenges** (Platform/Backend run→read→cite) correctly carved 2 keystones (h21yy7 ~20-file semantic migration, 8hmunr scale) rather than rushing a half-fix.

## What Didn't / Surprises
- **F-5 sweep narrowness recurred** (again): bd-qun6gg (2nd ContentHash test *project*), 8hmunr integration-test miss — my per-bead verify forks ran the *new-lock filter*, not the full unit suite / all test projects.
- **Clause-6 slip:** fxn2z3 tracker-closed but impl transiently reverted at commit time → not in HEAD (caught at the next verify). 
- **My mtime-gate misfire:** spawned a carrier treating Tests-in-a-long-CI-run as dead — self-corrected, converged, no file loss. Lesson: a full-CI run is a known-long-op = alive.
- **4 review/docs-tier sessions timed out overnight** (ProjectReviewer/SA/DocumentationWriter) — carried via clause-4 same-type sub-agents (real independent reviews). The autonomous run has no operator to restart sessions; carriers kept the pipeline moving.

## Recommendations
1. **[CONTINUE]** Layered verification + author≠impl non-vacuous locks + real-infra locks for external-system boundaries. Evidence: caught every material defect this sprint.
2. **[CHANGE]** PM verify-forks must run the **full affected unit suite** (and grep ALL test *projects*) on a contract change, not just the new-lock filter. Evidence: bd-qun6gg/8hmunr F-5 misses reached CRUCIBLE. (Already covered by f5-cross-project-test-sweep — reinforce, don't re-rule.)
3. **[CHANGE]** For P1-security real-infra locks, RUN before IMPLEMENT-complete, not defer to TEST. Evidence: hnx8gw valid-recovery bug would've been caught pre-HEAD.
4. **[TRY]** Route integrator commits through `bd-flush-guard.sh --verify-staged` uniformly (the reinstalled hook already helps) to end the export-race reconcile churn. Measurement: no reconcile commits next sprint.
5. **[CONTINUE]** Clause-4 carriers for terminated phase owners in autonomous mode — kept CLOSE-able through 4 dropouts.
