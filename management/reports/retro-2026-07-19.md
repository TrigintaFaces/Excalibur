# Retrospective — Sprint 892 (2026-07-19)

**Mission 240 — Correctness + Hardening Sweep (Backlog Drain 1 of 2).** Closed clean; all gates PASS.

## Metrics

- **Delivered (durable):** 3 P0s (keystone outbox CreatedAt/ordering, escrow M-of-N quorum, marker-seam un-fakeable) + yz7zz4 + 2 in-flight TEST-gate fixes (d0amws, n6uzrx) + CHANGELOG + ~15 non-P0 deltas.
- **Commits (post-FORGE view):** 16 — feat 1, fix 3, refactor 1, test 3, docs 1, chore/tracker 7. (Earlier IMPLEMENT commits precede this window.)
- **Gates:** TEST (build 0-err, unit suites GREEN, **2 real regressions caught+fixed**), VERIFY **11/11 full coverage**, SAMPLES (blast-nil), REVIEW_CODE **PASS 0-blocking**, REVIEW_ARCH **PASS 0-blocking**, DOCS (229-doc build), governance-stack PASS, samples **93/93**, AOT **0 warnings**. Every gate PASSED for real.
- **Carryover → S893:** the real P2/P3 tail + 1vxywb + authorization.md enum-fix + x481c9/4f239h + DX batch + haqhcm/0qyitl + misc (~40 items). Not rushed — deliberately deferred for fresh premise-gating.

## Test failure triage

- **In-branch/regression caught at gate:** 2 — `d0amws` (S891 bfak2b F-5 build-break) + `n6uzrx` (S892 rna328 ADR-142 §D7 governance) — **both fixed in-sprint** before REVIEW.
- Both would have failed `ReleaseBlocking.slnf`. The full-solution-build + unit-shards TEST bar caught what per-task validation missed.

## What worked well

- **Real-container keystone lock caught a 2-provider defect that 2 verifiers missed.** The PG/Oracle drain-reload `CreatedAt`/`SequenceNumber` drop was invisible to Backend self-verify AND SA seam-review; only the non-vacuous real-infra K1.1 lock surfaced it. Real-infra locks earn their cost.
- **TEST full-solution-build + unit-shards bar is load-bearing.** Caught 3 S891/S892 escapes (bfak2b + d0amws + n6uzrx) that per-task gates missed.
- **Clause-4 carriers + revival-safe TaskStop = zero-collision resilience.** Three review-phase sessions went down overnight (PR flap, SA false-dead, DocWriter dead 11.75h); all carried with genuine (not rubber-stamped) reviews, zero task collisions, zero stalls, sprint still closed clean.
- **Honest retractions reached the artifact.** Tests' src-side misdiagnosis on d0amws was retracted to the routing + the people before wasted work; SA's proof-of-life stood the carrier down before collision.
- **Single-actor reroute discipline** on d0amws (3 agents converged; PM ruled one owner) — zero double-edits.

## What didn't work

- **The `shipped-ddl-sweep` gate is broken/over-strict** — REFUSEs repo-wide on pre-existing unmapped shipped-DDL (`events`, `inbox_messages`, `LegacyCustomers`×4, `outbox_fence`, `outbox_dead_letters`), false-triggers on changelog *prose* mentioning column types, and scans the *working tree* so an incomplete carried change (1vxywb) blocks unrelated commits. Cost ~30 min of failed slow sweeps at DOCS/CLOSE. **Filed P2 gate-refinement bug for S893.**
- **The liveness discriminator (`REWAKE`-without-`START`) false-positives on subagent-heavy REVIEW/DOCS turns** — a review fanning parallel specialists runs ~30 min in one turn, freezing the heartbeat identically to a hang. Cost carrier churn (all recovered safely). → `/self-improving` clause.

## Surprises

- SoftwareArchitect's "hang" was a **false-DEAD** — a legitimate ~30-min single turn running two parallel specialist subagents. The mesh's own long-op signature is indistinguishable from death at short timescales.
- `shipped-ddl-sweep` false-triggers on the CHANGELOG *documenting* the very DDL change it's meant to police — the fix (reword prose) is orthogonal to the gate's real purpose.

## Recommendations

1. **[CONTINUE]** Non-skipped real-infra locks for provider-boundary P0s. *Evidence:* keystone K1.1 caught the 2-provider defect nothing else did.
2. **[CHANGE]** Promote the liveness-discriminator timescale clause into `verify-worker-liveness-before-dispatch` (via `/self-improving`): REVIEW/DOCS-with-subagents = known-long-op; check live subagent processes before declaring hung; multi-hour freeze = genuinely dead. *Evidence:* 3 false/real freezes this sprint, all timescale-distinguishable.
3. **[CHANGE/FIX]** Fix `shipped-ddl-sweep` early in S893 — add missing MAP_ROWS entries OR scope the trigger to real DDL-file changes (not prose / not working-tree pre-existing). *Evidence:* blocked every docs-site commit at close.
4. **[TRY]** S893 keystone = `0qyitl` MongoDb leader-fence P0 (unblocks the `haqhcm` epic). *Hypothesis:* unblocking the epic clears the largest carried cluster.

## Agent breakdown (praise-first)

- **TestsDeveloper (CRUCIBLE/TRACEPOINT):** the sprint's MVP — full-solution-build gate caught 2 real regressions, held CRUCIBLE honestly on a non-building HEAD, delivered 11/11 VERIFY coverage, and honestly retracted a misdiagnosis to the artifact. Model gate discipline.
- **BackendDeveloper:** delivered the keystone + rzr5zs quorum crypto; grounded challenges strengthened the marker-seam design.
- **FrontendDeveloper:** owned the d0amws + n6uzrx regression fixes single-actor, found the 2nd F-5 sibling, respected pacing. *Growth:* the rna328 sub-agent's within-deliverable inconsistency (faked Google SDK but subclassed AWS) — a single delegated task should apply the same seam pattern across all providers.
- **SoftwareArchitect:** thorough REVIEW_ARCH (0-blocking, security/AOT/WWMD), self-corrected seam rulings, and contributed the liveness-clause improvement.
- **ProjectReviewer / DocumentationWriter:** sessions failed overnight (infra, not conduct) — carried; their prior-phase contributions (seam reviews, docs validation) were solid.
