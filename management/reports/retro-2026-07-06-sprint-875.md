# Retrospective — Sprint 875 (Free OSS Operational Dashboard, waxs7f)

**Date:** 2026-07-06 · **Final HEAD:** `89880ea07` · **Baseline:** `ea55fd870`

## Commit analysis
- **26 commits** · +7,743 / −88 across 109 files (pure additive — greenfield package set).
- feat 10 · chore 7 · docs 4 · test 3 · fix 2.

## Sprint metrics
- **20/20 waxs7f lanes** integrated + closed. HOLD-SCOPE honored (no mid-sprint scope add).
- All gates GREEN: 10-shard CI ~109,540 pass / 0 regressions · traceability 100% · REVIEW_CODE +
  REVIEW_ARCH PASS 0-blocking · samples SUCCESS · governance 5/5 · AOT 0-warn.

## What worked
1. **[CONTINUE] Real-infra locks binding emitted behavior** — caught `bd-64b73f` (Postgres 42P08
   cross-provider filter, green-on-SqlServer/broken-on-Postgres). The `verify-against-real-infra-not-mock`
   discipline paid off again, exactly as designed.
2. **[CONTINUE] Integrator clean-rebuild + full-lane-stage discipline caught 3 self-inflicted defects
   pre-commit:** under-staged Abstractions DTOs (working-tree build masked a non-compiling committed HEAD),
   7henpa 80× RS0016 (incremental-build-stale hid it, `--no-incremental` surfaced it), and `.Spa` missing
   from the sln (caught by the `sync-ci-cd` governance gate). Every one caught before it reached a
   downstream gate.
3. **[CONTINUE] Structural design → near-zero review findings.** Auth boundary structurally enforced
   (mutating group unmapped unless enabled → 404/zero attack surface) + fail-open everywhere meant both
   reviews passed 0-blocking on the first pass. `enforce-invariants-structurally` in practice.
4. **[CONTINUE] TEST-phase coverage catch** — Tests found the `4e42zy` auth AC had no test and authored
   `DashboardAuthBoundaryShould` before REVIEW (forge cl.2 committed the delta pre-review).
5. **[CONTINUE] Idle-phase parallel planning** — CEO/PdM banked the entire next band (`66wjpx` provider
   expansion, `1lv5ux`, `nv3nou`, Marten) while TEST/SAMPLES ran. Zero wasted wall-clock.

## What didn't
1. **[CHANGE] CLOSE-gate auto-nudge storm — 3rd recurrence (S867 + S874 + S875).** The CLOSE phase task
   is re-dispatched every ~30s while multi-minute governance/samples/AOT gates run under an `in_progress`
   task, burning turns on ack-only churn. Posting an `in_progress` heartbeat before the gates (done this
   sprint) helps but does not stop it. **Now meets the ≥3× bar** → mechanical fix (see self-improving).
2. **[CHANGE] Stale MEMORY note persisted 2 sprints** — the "5 duplicate bd daemon procs — operator
   cleanup pending" was Bitdefender (`bdservicehost.exe`), not beads; carried in MEMORY + S874/S875 notes
   until SA ran the misidentification down. Cost an unnecessary operator escalation. MEMORY now corrected.
3. **[TRY] W2 SPA (6 lanes) shipped with 0 automated tests (`bd-1j3la3`)** — the largest coverage hole;
   no Vitest/Svelte harness exists. Tracked, not closed. Stand up SPA test infra before the next
   dashboard band (`nv3nou`) so UI lanes aren't test-dark.

## Surprises
- `.Spa` was missing from `Excalibur.sln` (I `dotnet sln add`-ed `.EventSourcing`/`.Tests` but `.Spa`
  slipped through). Reinforces: **run `sync-ci-cd` as a gate, don't eyeball** — the governance stack
  caught what a manual check would have missed.

## Recommendations (ordered by impact)
1. **[CHANGE]** CLOSE-gate nudge storm → mechanical fix (bead): server nudge-backoff for a gate-running
   phase owner, or client-hook suppress-when-`in_progress`. Evidence: 3 sprints. (self-improving below.)
2. **[CONTINUE]** Real-infra locks + integrator clean-rebuild/full-lane-stage — caught 4 real/self-inflicted
   defects between them this sprint.
3. **[TRY]** SPA test harness (Vitest) before `nv3nou`. Hypothesis: closes the single biggest coverage
   hole for dashboard UI work.
4. **[CONTINUE]** Structural-invariant design (unmapped-unless-enabled auth) → 0-blocking reviews.
