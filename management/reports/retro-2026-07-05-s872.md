# Retrospective — Sprint 872 (Remediation & Conformance)

**Date:** 2026-07-05 · **Mission:** 220 · **Final HEAD:** `f28faacc4` (baseline `5c84dc8a8`) · **Status:** CLOSED, all phases green, both reviews PASS

## Commit Analysis
- **65 commits** · 266 files · **+3,975 / −14,165** (net −10,190 — remediation/cut-heavy, as scoped)
- By category: chore 29 (mostly Beads tracker reconciles), feat 12, refactor 8 (AVW/serverless cuts), test 6, docs 6, fix 4

## Sprint Metrics
- **58 lane beads** (spec-decomposed from a drained remediation backlog). **56 delivered + 2 carved = 58 accounted (100%).**
- Carried → next sprint (tracked): `6v59o0` (Confluent decoder + provisioning orchestrator WIRE), `c4anmz` (dedup metric), `maf76z` (cross-provider inbox), `30y2xd` (dead-code cleanup).
- Gates: TEST 10/10 shards GREEN + Docker 6/6+2/2; VERIFY 0 AC gaps; SAMPLES 92/92; REVIEW_CODE PASS (0 blocking); REVIEW_ARCH PASS (0 blocking); governance stack PASS (359/359); AOT clean.

## Test Failure Triage
- **In-branch regressions caught by the TEST gate:** 4 compile-breaks (Cdc/Security S870-inherited; Transport.Tests + sample = 8d3vus CUT orphans) + 4 shard-regression clusters (7 tests: AdaptiveTtl options-validation, Postgres SaveSaga sibling-count, GooglePubSub oversized test-construction, E1 second-AddOptions) — **all fixed + re-verified GREEN.**
- **Flaky/env:** CI deadlock from ~50 zombie `dotnet` processes — cleared by reset; recommend periodic inter-agent cleanup.

## What Worked Well
- **Real-infra locks caught a runtime-fatal keystone defect** (`EventSerializationDefaults.MakeReadOnly` with no resolver → `TypeInitializationException`) that build-0/0 AND unit tests missed. Textbook `verify-against-real-infra-not-mock`. Coupling the transactional-inbox impl to its real-infra lock (not committing impl-alone) is what surfaced it.
- **verify-before-claiming + SA code-adjudication** on shard-3: Platform's investigation exposed a *mis-attributed* root cause (its own CUT blamed but a zero-ref dead interface), SA ruled on the actual code, Tests honestly retracted — no needless impl change to a fail-safe DoS guard.
- **Clause-4 carriers** covered TWO terminated review/docs sessions (ProjectReviewer, DocumentationWriter) with genuine independent run→read→cite work — the pipeline never stalled with the operator unavailable. This validates `verify-worker-liveness-before-dispatch` clause 4 end to end.
- **Seam-first discipline** on the exactly-once keystone: SA rejected MSDTC, self-corrected an `IUnitOfWork` boundary violation, and landed a BCL-`IDbTransaction` seam BEFORE impl — zero rework.

## What Didn't Work / Recurring
- **F-5 cross-project sweep too narrow — 3× this sprint alone.** CUTs deleted src but left orphan **test files** (8d3vus: 9 files), orphan **samples** (8d3vus sample + cicd-sync), and stale **sibling-count** assertions (kmw1dh `CompletedAt`), plus the saga-correlation/reminders CUT orphans. All caught late by the full-solution build, not the IMPLEMENT-phase sweep.
- **Options-validation guard blind spot:** a 2nd `AddOptions<T>()` call site for an already-validated `T` (qzp9k7:67) escaped the per-bead mental model — caught by the shard guard.
- **Env instability:** zombie `dotnet`/csharp-ls DLL-lock contention recurred across the whole sprint.

## Recommendations
1. **[CHANGE — PROMOTE]** F-5 cross-project-sweep must grep the test **directory** mirroring a cut namespace + orphan **samples** + **sibling-count** deltas, not just contract tokens. → clause on `f5-cross-project-test-sweep.md` (see `/self-improving`). Evidence: 3× recurrence in one sprint.
2. **[CONTINUE]** Couple safety-critical impl to its non-skipped real-infra lock before integration. Evidence: caught the runtime-fatal serializer defect.
3. **[CONTINUE]** Clause-4 carriers for terminated phase owners. Evidence: 2 clean coverages this sprint.
4. **[TRY]** Periodic inter-agent `dotnet`/csharp-ls process cleanup to prevent CI deadlock. Hypothesis: removes the recurring env stall.

## Rule-Promotion (deferred to /self-improving)
- PROMOTE: F-5 test-directory + orphan-sample + sibling-count clause (≥3× bar cleared).
- CONSIDER: OptionsValidateOnStart per-call-site note (1st occurrence — likely track, don't mint).
- DECLINE new files for positives that VALIDATED existing rules (verify-before-claiming, clause-4-carrier, seam-first all worked as written).
