# S888 Retrospective — Provider Parity, Exactly-Once Durability & Design-Audit Hardening

**Mission 236 · SHIPPED (keystones) @ `0e85fd919` · baseline `8997102bf` · 2026-07-14 (autonomous overnight ops)**

## Commit Analysis
- **15 commits** since baseline (lean — scope shrank at SPEC as designed for a keystone-focused sprint).
- By category: docs 6, chore 4, test 2, fix 2, feat 1.
- The high docs/chore ratio = correctness + provider-parity keystone sprint (integration + fencing +
  docs), not a feature sprint — expected for the theme.

## Sprint Metrics
- 12 phases: 11 completed, STRATEGY skipped (continuation sprint, no new bet). All ran their owners.
- Keystones delivered + closed (verified at committed HEAD): `f5zutu` (P0 outbox fencing atomic CAS),
  `uw1nv4` (P1 sd36sc server-side fencing), `rjolfk`/`y1moc0`/`3q1jtm` (inbox transactional parity +
  sent-tracking capability), `5fswhd`/`0qyitl` (Mongo LE durable fencing).
- **Open in-scope P0 at close = 1** (`15ph5g`, Dijkstra D1 structural-marker hardening) — carried to S889
  as lead item (NOT delivered this sprint). `in_progress` = 0 at close.

## Quality Gates (all PASS)
- Full CI **10/10 shards GREEN**, 0 regressions (shards 3 & 8 exit=1 = known librdkafka teardown leak
  `moe5g9`, not S888 — no transport code touched).
- Real-infra Docker: all provider-parity deliverables GREEN (SqlServer/Postgres inbox exactly-once;
  PG/Oracle outbox fencing; Mongo LE fencing). y1moc0 *fixed 3* previously-red PG conformance facts.
- VERIFY full-coverage 0 critical gaps · REVIEW_CODE + REVIEW_ARCH **APPROVE**.
- CLOSE gates: governance-stack **PASS** (389/0) · samples **93/93** build+smoke · AOT **0** IL warnings.

## What Worked Well
1. **Independent REVIEW_CODE caught a real exactly-once P0 (`f5zutu`) that TEST 10/10 + full-coverage
   VERIFY both passed.** The fencing CAS was a check-then-act TOCTOU split across two DB round-trips a
   demoted leader could interleave. Same **"green gates miss the seam defect"** class as S887 — the
   reviewer reading impl on committed mainline is the highest-leverage gate. Already covered by
   `enforce-invariants-structurally` + `gate-full-guard-suite` + `verify-against-real-infra-not-mock`.
2. **The real-infra integration gate caught a bug in the FIX.** The first Shape-2 fenced claim called
   `BeginTransaction()` on an unopened shared `_db.Connection` → `InvalidOperationException`. NO-GO at the
   gate → Platform re-folded to a single-statement wCTE (no BeginTransaction) → re-gated GREEN on real
   PG 3/3 + Oracle 3/3. `verify-against-real-infra-not-mock` + `clean-rebuild-before-trusting-locks`
   earned their keep — a mocked lock would have certified the broken connection handling.
3. **Gate-version-drift caught before commit.** The coupled tree was re-folded 6→7 files AFTER an earlier
   GO; caught via git file-count + mtime + the implementer's handoff, HALTED, re-gated the *actual*
   current tree before committing. Prevented committing an un-gated/non-compiling tree
   (`forge-integration-conventions` clause 1 + `clean-rebuild` working-tree-vs-SHA, working as designed).
4. **5-party diagnose-open → SA-ruled seam → single-actor fix → independent author≠impl lock.**
   `issue-remediation-protocol` ran clean: everyone diagnosed read-only, SA ruled the seam (high-water in
   a separate control table, one atomic statement), Platform alone implemented, Tests independently
   authored the RED-on-pre-fix lock. Zero collisions.
5. **HIVE mesh + mtime-gate liveness held through the overnight run.** DocWriter's ~36-min DOCS turn read
   `offline` on OPCOM heartbeat (frozen UTC ts) but doc-file mtimes (16:37–16:50, CHANGELOG 4 min fresh)
   proved it alive → correctly NOT woken (no collision). mtime ground-truth overrides the heartbeat
   signal — `verify-worker-liveness-before-dispatch` mtime-gate, as in S887.

## What Didn't Work / Friction
1. **An in-scope P0 (`15ph5g`) shipped nothing.** The Dijkstra D1 structural-marker-hardening slice
   (Lane E design-audit) was scoped (S888 §5) but the sprint's finite capacity went to the fencing/inbox
   keystones. It's structural hardening (not a confirmed active leak), correctly carried — but a P0 in
   the plan that produced no commit is a scoping-vs-capacity signal: the design-audit epic (`haqhcm`)
   should be its own sprint spine, not a band-filler behind two keystones. **Recommendation applied:
   S889 leads with 15ph5g.**
2. **Integration/close commit hooks time out under Bitdefender.** The clause-6 HEAD-verify Bash hit the
   2-min tool limit (exit 143) on the trailing `log-discovery` — the git+python verify had already
   printed correctly; the discovery was captured in the review + channel post. Commits themselves used
   300000ms and were clean. Recurring env cost, already mitigated.
3. **`blocking-bead-gate` miscount recurred** — printed "3 live P0(s)" while real open-P0 = 1 (`15ph5g`),
   every commit. Second sprint running (S887 → S888). Known-inert gate (`jslh06` P3). Non-blocking; PM
   verified by direct `bd` query, never trusted the gate. **Now 2× — one more recurrence crosses the
   rule/fix threshold; the P3 fix should be pulled forward.**

## Test Failure Triage
- In-branch (caused by sprint work): 1 real defect (`f5zutu` fencing TOCTOU) — caught by REVIEW_CODE,
  fixed `d2ed0081e`, re-gated GREEN. Plus the fix-bug (BeginTransaction) caught at the integration gate,
  re-folded to wCTE.
- Pre-existing: 15 PG + 2 Oracle outbox conformance data-fidelity fails — tracked (`4z0iid` P2), NOT
  S888-caused (worktree-proven 18→15 on PG). Emulator-absent integration reds (DynamoDb/Firestore/Cosmos)
  — env, tracked.
- Flaky: 0 new. librdkafka teardown leak (`moe5g9`) unchanged.

## Recommendations
1. **[CONTINUE]** Independent reviewer reads impl on committed mainline, not green gates. Evidence: caught
   the `f5zutu` exactly-once P0 post-VERIFY. Highest-leverage gate two sprints running.
2. **[CONTINUE]** Real-infra integration gate on the exact committed tree + gate-version-drift re-check.
   Evidence: caught the BeginTransaction bug in the fix AND the 6→7 re-fold before commit.
3. **[CHANGE]** Give the design-audit epic (`haqhcm` Dijkstra / `mgwuiu` Metz / `8cnpj4` Liskov) its own
   sprint spine rather than band-filling behind keystones. Evidence: `15ph5g` P0 produced no commit when
   scoped as tail. Effort: 1 SPEC decomposition. Applied: S889 leads with 15ph5g.
4. **[TRY]** Pull the `blocking-bead-gate` P0-count fix (`jslh06`) into S889. Hypothesis: removes the
   recurring false-carryover noise. Measurement: gate line matches `bd list --status open --priority 0`.
   Evidence: miscount recurred S887→S888 (2×).

## Rule-Promotion Verdict
**0 new rules.** Overlap check (`rule-promotion-gate`, default NO):
- Independence-caught-seam-defect, real-infra-gate-caught-fix-bug, gate-version-drift → all covered by
  `enforce-invariants-structurally`, `verify-against-real-infra-not-mock`, `clean-rebuild-before-trusting-locks`,
  `forge-integration-conventions`, `gate-full-guard-suite`. No new facet.
- mtime-overrides-heartbeat → `verify-worker-liveness-before-dispatch` mtime-gate (already owns it).
- In-scope-P0-carryover → sprint discipline + `scope-the-keystone-to-its-real-extent` (band-vs-capacity).
- blocking-bead-gate miscount (2×) + Bitdefender timeout → single-tooling/env items, tracked
  (`jslh06`), under the ≥3× recurrence bar. Track, don't promote.
