# Retrospective — Sprint 857 (EPIC w2zq7d Migration Tooling)

**Date:** 2026-06-28 · **PM:** ProjectManager (DEBRIEF) · **Mission 205** · **Duration:** ~6.5h (15:44 → 22:11) · **Scope mode:** Expansion (WIDE, operator-directed)

---

## Commit Analysis

**Period:** 2026-06-28 (sprint day) · **S857-tagged commits:** 21 (`12761da48` plan → `e19ff4c34` close)

By category (today):
- feat: 6 · fix: 4 · chore: 6 · docs: 3 · test: 2

Keystone footprint:
- `37b46aa62` — MediatR migration keystone: **61 files, +3364 / −71** (Compat.MediatR shim + generator + WS1/WS2/WS3 + samples/tests/CI-sync)
- `3c69635b7` — TEST regression-fix: 14 files (keyed-DI guard rename + packable-aware CI-sync test + PublicAPI baselines)
- `e19ff4c34` — CLOSE beads reconcile + hook-staged PublicAPI baseline promotions (36 files, mostly tooling-generated)

## Sprint Metrics

- **Phases:** PLAN→SPEC→GUIDE→IMPLEMENT→TEST→VERIFY→SAMPLES→REVIEW_CODE→REVIEW_ARCH→DOCS→CLOSE→RETRO. STRATEGY skipped (operator-ruled bet).
- **Keystone:** EPIC w2zq7d delivered — MediatR-compat shim, Roslyn analyzers/code-fixes (EXMIG0001-0004), MassTransit path, ported sample, ADR-341.
- **VERIFY:** FULL coverage (traceability matrix `management/reports/s857-spec-verify-w2zq7d.md`).
- **Reviews:** REVIEW_CODE + REVIEW_ARCH both APPROVED, 0 blocking (1 non-blocking P3).
- **Gates at close:** Governance 359 proj / 0 err · Samples 93/93 · AOT 0 IL warnings — ALL GREEN.
- **Carryover → S858:** `ajt1iy`, `bd-d2kokz`, `bd-bo1axj`, `ewo4at`/`c01ymk`/`fcdn6k`, `fz8oyc`/`vfssk3`, `2tf65w`/`3ws5u7`, `vbv0at` — all tracked, none dropped.

## Test Failure Triage Summary

```
In-branch (caused by sprint work):  3 keystone-integration regressions — ALL FIXED @3c69635b
                                     (bd-ybem93 keyed-DI guard, CI-sync pack filter, PublicAPI baselines)
Pre-existing (already broken):       1 env (AOT vswhere-PATH) — Beads task created (bd-bo1axj)
Flaky (intermittent):                1 (RetryMiddlewareMetricsShould.RecordRetryExhausted) — known/tracked
Fixed (bonus):                       audit-lane carryover (g3b7vo/d7ziag/d0f69o)
```

## What Worked Well

- **Grounded IMPLEMENT challenges caught spec staleness early.** The spec-vs-MediatR-12.5 drift (C1 `RequestHandlerDelegate(ct=default)`, C4 `where TRequest:notnull`, C5 closed `AddBehavior` overloads) and the generator-composition keystone were all surfaced and resolved at IMPLEMENT, not REVIEW — exactly `scope-the-keystone-to-its-real-extent` working as designed.
- **Layered review held.** REVIEW_CODE (boundary/visibility/AOT) + REVIEW_ARCH (structural ruling verification, run→read→cite) gave 0 blocking with independent confirmation of every ADR-341 seam ruling as-built.
- **Carrier-session pattern.** REVIEW_CODE and DOCS were carried by registered carrier sessions when the real workers were offline (`verify-worker-liveness-before-dispatch` clause-4) — pipeline never stalled.
- **All 3 governance gates run fresh at CLOSE** rather than trusted from mid-sprint — caught the AOT env gap before it could be mis-reported as a code defect.

## What Didn't Work

- **AOT gate fragility (env, not code).** The native-link step depends on `vswhere.exe` being on PATH; it isn't in the default dev/CI shell, so the gate failed misleadingly (0 IL warnings but publish exit 1). Pre-existing on committed HEAD. → `bd-bo1axj`.
- **Pre-commit hook auto-stages PublicAPI baselines repo-wide.** The CLOSE tracker-reconcile commit unexpectedly swept ~33 PublicAPI files + a lock file (hook re-staged them after `git reset`). Net harmless (legitimate Unshipped→Shipped promotions + branch .NET-update churn), but it defeats `forge-integration-conventions` clause-5 scoped-commit intent. Worth documenting so integrators expect it on this branch.
- **Beads daemon flush race at CLOSE.** `bd sync --flush-only` staged a pre-creation snapshot; the newly-filed `bd-bo1axj` was missing from committed HEAD until an authoritative `bd export --no-auto-import` re-flush (`bd-flush-then-commit` clause-6 in action — caught by the committed-HEAD verify).

## Recommendations

1. **[CONTINUE]** Run all governance gates fresh at CLOSE, and always verify the close reached committed HEAD (`git show HEAD:.beads/issues.jsonl`).
   *Evidence:* caught both the AOT env gap and the `bd-bo1axj` flush race this sprint.

2. **[CHANGE]** Make the AOT validation script self-locate `vswhere.exe` at the known VS Installer path instead of depending on PATH.
   *Evidence:* gate failed at native-link despite clean managed AOT analysis. *Effort:* small (script-only). *Tracked:* `bd-bo1axj`.

3. **[TRY]** On branches where a pre-commit hook auto-stages PublicAPI baselines, integrators should expect and accept the baseline-promotion sweep (or pre-empt by staging baselines deliberately) rather than fight it with `git reset`.
   *Hypothesis:* removes the surprise + double-commit churn seen at CLOSE. *Measurement:* single scoped close commit next sprint.

4. **[CONTINUE]** Keep the grounded-challenge-at-IMPLEMENT discipline for keystone scope/seam staleness.
   *Evidence:* C1/C4/C5 spec drift + generator composition all caught pre-REVIEW.

## Agent Breakdown (praise)

- **SoftwareArchitect (ORACLE/COMPASS):** structural as-built verification of every ADR-341 ruling, run→read→cite — model REVIEW_ARCH.
- **TestsDeveloper (CRUCIBLE/TRACEPOINT):** found 3 keystone-integration regressions at shard-1, re-verified all fixed, FULL traceability matrix.
- **ProjectReviewer (SENTINEL):** independent carrier-session review, non-vacuous CI-sync mechanism verification.
- **Frontend/Platform Developers:** self-corrected the Guard-2 CI-sync design flaw + packable-aware fix, single-actor.
- **DocumentationWriter (CHRONICLE):** full migration docs + EXMIG diagnostics, docs-site GREEN, every example validated against src.
