# Retrospective — Sprint 858 (2026-06-29)

**Sprint:** S858 — Advertised-unwired consolidation + S857 correctness tail (WIDE ~70-80); lwoxbh/ADR-336 + audit-2026-06 cluster
**Baseline:** `e19ff4c34` → **Close:** `33d814b01`

## Commit Analysis
- **Total:** 30 commits · +6819 / −3215 lines
- **By category:** fix 8 (27%) · docs 9 (30%) · chore 5 (17%) · test 4 (13%) · refactor 2 (7%) · feat 2 (7%)
- Profile matches intent: a correctness/consolidation sprint (fixes + wiring + docs), not greenfield feature work.

## Phase Outcomes (12 active phases; STRATEGY skipped — CEO offline at launch)
All phases completed and auto-advanced. Gates at close:
- Governance 359/359 (0 err/warn) · Samples 93/93 · AOT 0 IL2xxx/IL3xxx · docs-site 216/0
- Full-CI 10-shard GREEN · REVIEW_CODE + REVIEW_ARCH ✅ 0 BLOCKING / 0 P1 each

## Test Failure Triage
- **In-branch (caused by sprint work):** 2 deterministic — both fixed (bd-jv2toc 3-provider fail-open; bd-c6bjc3 stale F-5 sibling). YES.
- **Pre-existing/flaky:** 1 — `RetryMiddlewareMetricsShould.RecordRetryExhausted` (parallel-load only). Tracked bd-76xkmi. YES.

## What Worked Well
- **issue-remediation-protocol on the jv2toc CI finding** — diagnose-open, PM single-fixer-by-assignment (Platform), independent regression lock (Tests). Zero file collisions despite Backend also volunteering.
- **Blast-radius grep (F-5 enumerate)** — caught jv2toc was a 3-provider family bug, not Lambda-only. A narrow fix would have shipped Azure/GCP still fail-open.
- **commit-surface-before-parallel-edits** — integrator committed the impl (fd04a4fbd) ahead of the test set, protecting uncommitted work during Tests' cp-backup non-vacuity (no `git checkout` on impl).
- **Structural fix** — `ComputeExecutionTimeout => Max(Zero,…)` makes fail-open inexpressible (enforce-invariants-structurally); both reviewers independently confirmed.
- **Layered independent review** — REVIEW_CODE (6 agents/48 files) + REVIEW_ARCH (4 agents/68 files), both 0 blocking.

## What Didn't Work / Friction
- **F-5 cross-project sweep narrowness, AGAIN** — the stale CB sibling test (`DistributedCircuitBreakerShould`) was not flipped when zxb7fp's windowed-ratio rewire landed at IMPLEMENT; only full-CI (TEST phase) caught it. Recurring class (S840→S855).
- **bd-flush stale-snapshot recurred** — the first tracker commit (46cd2c79e) staged a pre-close jsonl; caught at forge clause-6 (committed-HEAD read) and fixed via `bd export --no-auto-import`. Known daemon-mtime race.
- **AOT gate needs `vswhere.exe` on PATH** — native-link `MSB3073 exit 123`; environmental, not a code defect (trim analysis passed). Recurring (bd-2s2jgc).

## Surprises
- Full-CI surfaced 2 deterministic fails that every per-unit gate passed — both were contract-change blast-radius (windowed CB + 500ms reserve), reinforcing that contract changes need the IMPLEMENT-phase F-5 sweep, not the TEST backstop.

## Recommendations
1. **[CONTINUE]** issue-remediation-protocol single-fixer + independent lock. Evidence: zero collisions on a 3-file fix with 3 agents converging; independent lock proved non-vacuous across all 3 providers.
2. **[CHANGE]** Run `eng/ci/f5-sweep.sh --base origin/main` at IMPLEMENT for any type-contract change (e.g. zxb7fp windowed CB). Evidence: stale CB sibling reached TEST when an IMPLEMENT-phase sweep would have caught it. Effort: low (mechanical gate already exists).
3. **[CONTINUE]** forge clause-6 committed-HEAD verification. Evidence: caught the bd-flush pre-close jsonl snapshot before it shipped a desynced tracker.
4. **[TRY]** Add VS Installer dir to the AOT gate's PATH resolution (bd-2s2jgc). Hypothesis: removes the recurring manual vswhere workaround. Measurement: AOT gate passes clean without manual PATH prepend.

## Agent Breakdown (praise-first)
- **TestsDeveloper (CRUCIBLE):** ran full-CI, STOP+reported per protocol, authored non-vacuous locks via cp-backup. Model TEST discipline.
- **PlatformDeveloper:** blast-radius grep + structural fail-closed fix across 3 providers, 0W/0E.
- **SoftwareArchitect / ProductManager:** fast, grounded structural + AC rulings (fail-closed, provider-agnostic).
- **BackendDeveloper:** raised the fix, then clean disagree-and-commit stand-down when PM assigned Platform's lane.
- **ProjectReviewer + SA reviews:** independent, full-implementation reads — not rubber-stamps.
