# S891 Retrospective — DEBRIEF (Instrument Repair Wave 2 + Correctness Sweep)

**Closed:** 2026-07-18 · **Close commit:** `a2b4a9bfb` · **Mission:** 239 · **Baseline→HEAD:** `5a44f8f3e → a2b4a9bfb`

## Outcome

**GREEN, partial-scope.** Delivered the Wave-0 thesis (the instruments the sprints themselves run on) + 4 correctness
changes; two correctness lanes carved to S892 because their owners hung mid-IMPLEMENT.

| | Result |
|---|---|
| **Delivered** | Lane-G harness/mesh/gate — 4 Wave-0 P0s (`jxp2yq` 4-gates→CI orchestrator, `hv8fjh` flush fail-loud, `0p6z0v` liveness probe, `9pxz2t` poller-leak guard) + `w1u1c9` ring-repair; 5 harness self-tests non-vacuous (mutant-proven). 4 .NET: `bfak2b` env→IOptions, `5uajzo` fail-closed eviction, `ywodwj` TimeProvider seam, `xeo795` saga-timeout concurrent-claim lock. |
| **TEST** | 5601 passed / 0 failed / 0 skipped |
| **REVIEW** | REVIEW_CODE + REVIEW_ARCH both PASS, 0 blocking |
| **Gates at close** | governance 389/0/0 · samples 93 smoke/0 fail · AOT 0 IL |
| **Carried to S892** | Lane O+P (outbox + projection/store-DI, Backend) · Lane T (tenancy/crypto/erasure, Tests) |

## What went well

1. **Wave-0 landed first and clean** — the P0 instruments/mesh fixes (the ones that gate every sprint) shipped and were
   proven non-vacuous with safety∧liveness arms. The autonomous loop is now repairing the very tools it depends on.
2. **Clause-4 carry worked again** — SPEC was clause-4-carried when ProductManager hung; the sprint didn't stall.
3. **Carve-don't-stall held** — two undelivered lanes were carved to S892 cleanly rather than blocking the close.
4. **Close was smooth** — all three governance gates GREEN on the first run; discovery-bead reconcile clean.

## What recurred (already-covered by promoted rules — validation, not new rules)

1. **Mid-turn worker hang** (`0p6z0v` class): BackendDeveloper **and** TestsDeveloper both hung through IMPLEMENT
   (REWAKE-without-START; hook exits, agent never takes its turn). This is the exact failure the S890 promotion into
   `verify-worker-liveness-before-dispatch` (three-state idle/hung/terminated model, operator-only recovery) describes —
   and S891's Wave-0 shipped the mitigations (poller-leak reap, liveness probe, ring self-heal). Both revived post-sprint.
   **No new rule** — the recurrence validates the promoted three-state model and the carve response.

## self-improving / rule-promotion-gate decision

**Default honored: NO new rule.** Overlap check — every S891 pattern (worker hang, clause-4 carry, carve-don't-stall,
gate non-vacuity) is already covered by promoted rules (`verify-worker-liveness-before-dispatch`,
`forge-integration-conventions`, `gate-full-guard-suite`, `testing-patterns §3`, `enforce-invariants-structurally`).
Recurrence count for any *uncovered* facet = 0. Nothing clears the ≥3×-recurrence + non-coverage bar. The right response
was **building the Wave-0 fixes** (structural), which S891 did — not adding prose.

## Carryover into S892 (planned at 150 beads — final-two-sprints directive)

- Lane O+P beads (outbox correctness, fence-CAS, projection/store-DI wiring) — re-enter as `open`.
- Lane T beads (narrow DTOs, tenant markers, key-escrow quorum, crypto-shred honesty) — re-enter as `open`.
- 6 stale DX/docs beads reset to `open` (5pdhaw, h4wem2, sbtrxn, pz346x, 5hskwf, 4p2p6d).
- `vfssk3` — kept-open by reconcile (self-declared resolved, cites no commit); manual triage in S892 SPEC.
