# Retrospective — Sprint 886 (Correctness & Integrity)

**Closed:** 2026-07-13 · **Verdict:** GREEN · **Baseline:** `78931b777` → HEAD `b630622fd`

## Commit chain (remediation + close)
- `46de73490` fix(S886): saga tenant-isolation DI wiring — 7 factory sites resolve ITenantContext + real-DI liveness lock [bd-rw2ull]
- `022fbafb0` test(S886): adapt moved keyed-DI locks to unit-shard projects [bd-5nw7xa]
- `74b6e7808` chore(S886): close rw2ull + 5nw7xa + carry review-filed beads
- `27c2aa0f1` test(S886): Oracle LoadAsync materialization + UTC timestamp round-trip regression lock [bd-1m19p6]
- `b630622fd` chore(S886): CLOSE — governance/samples/AOT GREEN, Test Results + carryover [SUNSET]

## Gate results
TEST 119,277 pass / 1 pre-existing flaky / 0 regressions · VERIFY FULL COVERAGE · SAMPLES 93/93 build+smoke · REVIEW_CODE PASS · REVIEW_ARCH PASS · DOCS zero-drift · CLOSE gates governance/samples/AOT all exit 0.

## What worked
- **REVIEW_CODE earned its seat.** It found a P0 (`rw2ull`) that the 119k-test full CI AND a FULL-COVERAGE VERIFY both passed: saga tenant-isolation was inert at the DI layer (factories omitted `ITenantContext` → `TenantScope.FromContext(null)=None` → no row-level tenant predicate → cross-tenant leak) behind a lying capability-marker. The existing wiring lock hand-injected `new FixedTenantContext()` the factory omits — non-vacuous for store-SQL, **vacuous for wiring**. Independent human-grade review caught what green gates could not.
- **Grounded challenges materially improved outcomes:** Backend's F-5 found the fix is **7 sites, not 5** (the 2 extra are the *primary* `AddSaga().AddX()` builder path); Tests proved RED-non-vacuity against committed HEAD via a throwaway `git worktree` (never reverting uncommitted work); wzjj2w flipped SA's CUT ruling to WIRE on a re-read.
- **Clause-4 carriers kept the overnight mission moving** when SoftwareArchitect (~4h) and DocumentationWriter (~9h) froze — and the TaskStop-on-revival discipline averted collisions when both revived.

## What didn't (and the fix)
- **Integrator footgun (1×):** commit1's `git add` of in-scope files also captured 2 pre-staged `git mv` renames (from Tests) as **pure moves without their content-adaptation edits** → committed HEAD briefly held moved test files lacking their edits until commit2 landed them. Working-tree-vs-committed-SHA hazard. Fix: when the tree carries pre-staged renames from another actor, land a rename's MOVE and its CONTENT edits in ONE commit.
- **`bd sync --flush-only` exit-0 no-op:** returned 0 but did not write the closes to the tracked jsonl; `bd export --no-auto-import` was required. Exit-code ≠ effect.
- **Stale-message backlog:** ~150 S882 (07-10) `ackRequired`-any-age messages resurfaced overnight and had to be batch mark-read + acknowledged. Recommend a message-TTL / Stop-hook age filter.

## Recurrence signal for /self-improving
- **Wiring-inert-behind-a-passing-lock** now spans S871 (half-wire) → S873 (WIRE lock must resolve through REAL DI) → S886 (rw2ull) — **≥3×, same class.** Candidate for a strengthening clause: a capability-marker attestation must be *structurally* tied to the inner consumer actually receiving the dependency, not separately registerable.
- **TaskStop-on-revival for clause-4 carriers** — 2× this sprint (SA + DW). Candidate clause for `verify-worker-liveness-before-dispatch`: re-whois before spawn; stop the carrier the instant the real owner revives (real owner wins).

## Carryover → next sprint
P1 Oracle correctness cluster (1m19p6, u4x8sb, tfszov, y5tn3e, l9c3cv, ljbwh8, uysw7y, vfssk3); P2 3ttcqv (DB-backed saga behavioral test); review fast-follows tj6qvl/vttjcz/oqfo8r; 2 pre-existing doc phantoms; blocked P0s 88xrgq/4uubd1; dedup prd3xg~xx6wd8.
