# Retrospective — Sprint 860 (Provider Conformance Parity + P1 Carryover)

**Date:** 2026-06-29 · **Range:** `060564a57..4df1641ad` · **Mode:** sprint-860

## Commit Analysis
- **Total commits:** 23 · **Files changed:** 234 · **+10,525 / −1,059**
- By category: test 6, fix 5, chore 5, feat 3, refactor 2, docs 2

## Sprint Metrics
- **Delivered (closed):** 54 beads · **Carryover → S861:** 19 · **Re-scoped/phantom-closed:** ~6 (cloud-native outbox ×3, `fljcmo`/`t4a3rk`)
- **Scope shape:** keystone `fc1c8a` decomposed to ~29 conformance derivers + 6 P1 carryover + 12-bead platform lane + audit-2026-06 subset. Band corrected mid-SPEC (operator flagged under-band → pulled audit cluster).
- **Mission:** PLAN→SPEC→GUIDE→IMPLEMENT→TEST→VERIFY→SAMPLES→REVIEW_CODE→REVIEW_ARCH→DOCS→CLOSE→RETRO (STRATEGY skipped).

## Gates
- TEST 10/10 shards GREEN (Unit 35,386/0, Conformance 163/0, Deterministic 46,618/0, AsyncRisk 13,282/0, Perf 89/0).
- VERIFY full traceability (PdM sign-off) · SAMPLES 93/93 · REVIEW_CODE APPROVED (1 blocking fixed) · REVIEW_ARCH APPROVED 0-blocking · governance-stack PASS 0/0.

## Test/Review Failure Triage
- **In-branch caught + fixed:** 3 — `ycaxgt` (governance: concrete-SDK fake), `g51j5z` stale F-5 sibling, `bdzajd` (Sqlite concurrency-conflict re-read, REVIEW_CODE blocking).
- **Flaky:** 1 — `0vdh57` (order-dependent, P3 tracked, non-blocking).
- All blocking issues resolved before close; none shipped.

## What Worked Well
- **Keystone-decompose-at-SPEC + premise-gate:** SagaStore "zero derivers" premise corrected early; cloud-native outbox mis-scope (`ICloudNativeOutboxStore` ≠ `IOutboxStore`) caught at IMPLEMENT, not shipped.
- **Coupled-set integration discipline:** surgical staging caught 3 would-be-broken commits pre-HEAD (4bve3g test w/o impl, fhwpsh deletions w/o PublicAPI, vacuous lock).
- **Non-vacuity rigor:** Tests RED-proved its own committed lock vacuous; team converged (via grounded challenges + clean disagree-and-commit) on extract-to-internal-method + direct unit-test rather than ship a vacuous/flaky lock.
- **Layered review caught the real bug:** REVIEW_CODE found the `bdzajd` Sqlite throw-instead-of-conflict that 73k GREEN tests missed.

## What Didn't Work
- **Catch-breadth thrash (B1):** narrow↔broad flipped 3× (note→challenge→reverse→concede-crossed) before settling broad. Cost several round-trips.
- **Shared-tree probe collision:** ProjectReviewer's non-vacuity mutate-restore ran on the shared working tree → build-breaking collision with Backend/Tests.
- **PM seam-suggestion misfires:** PM's pre-seed lock idea (and an OnBeforeInsert hook) both couldn't reach the defended code-19 path (structurally unreachable) — burned cycles before SA's extract ruling.
- **Tracker-jsonl lag:** the daemon repeatedly left `bd sync --flush-only` a no-op; needed `bd export --no-auto-import` before staging the tracker each integration.

## Recommendations
1. **[CONTINUE]** Coupled-set surgical staging + integrator re-verify — caught 3 broken commits + the blocking bug. Evidence: 0 broken commits reached HEAD across 13 integration passes.
2. **[CHANGE]** **Rule seam error-handling-breadth ONCE, up front** (owner: SA) when extracting/adding an error path — the B1 narrow↔broad 3× thrash was pure round-trip cost. Effort: low (a single GUIDE/REVIEW ruling).
3. **[STOP]** Running non-vacuity mutate-restore probes on the **shared** working tree. Always use a private `git worktree`/`cp` backup. Impact: eliminates build-breaking collisions during review. (Promote to `commit-surface-before-parallel-edits`.)
4. **[TRY]** When a defensive code path is **unreachable-by-construction** for static/real-concurrency tests, **extract it to an internal method + direct unit-test** (SA's B1 disposition) instead of forcing a vacuous integration lock. Measurement: non-vacuous RED-on-pre-fix proof.
5. **[CHANGE]** Integrator routine: `bd export --no-auto-import -o .beads/issues.jsonl` (NOT `flush-only`) before staging the tracker — the daemon no-ops flush-only under multi-worker load.

## Carryover → S861
Heavy multi-provider cluster (`bkra3g` safety/CAS + ccn3qt/1wlxlz/4xnwo9/m0rm5r/5wo4w2); audit-2026-06 remainder (13, unassigned); cloud-native (`wxw6c3`+`38fdb5`/`c4ci4r`/`pkkx7x`/`ajt1iy`); `dhsik1` epic (decompose); P3 review follow-ups (`fcqe7y`/`koxhtr`/`223uof`/`wrylil`/`0vdh57`).
