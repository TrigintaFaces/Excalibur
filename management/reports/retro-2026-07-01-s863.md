# Retrospective — Sprint S863 (Focused Correctness & Advertised-Wiring)

**Date:** 2026-07-01 · **PM:** ProjectManager · **HEAD:** `583484f66`

## Commit Analysis
- **14 commits** (S863 range `9f8f1999c..HEAD`, excl. S862 reconcile) · **91 files, +3882 / −646**
- Features 3 (payload guard, pepper, LE event) · Fixes 3 (sqlite, gate-vacuity, samples) · Test 1 (11 stale-sibling fixes) · Docs 4 · Chore/build 3
- Single-author integration (PM); worker authoring across Backend/Tests/DocWriter carrier.

## Sprint Metrics
- **Planned:** 6 beads (focused, operator-chosen scope). **Delivered:** 11 (6 planned + wr3tuo sub + bk59dc/8w5izn/xeb2gc/13dbkp review+test fixes).
- **Completed:** 11/11 in-scope. **Carryover:** w6hoye (remaining scrub) + c88nux (slnf skill rename) — both tracked, non-blocking.
- **Mid-sprint additions:** all blocking-bug/review-fix class (sanctioned), none feature creep.
- **Priority:** P1 keystone (ydvyi7) + P1 gate (78z3kq) delivered; P2 cluster delivered.

## Quality / Gates
- Full CI **10/10 shards GREEN** · REVIEW_CODE **PASS** · REVIEW_ARCH **APPROVED** · Governance **5/5** · Samples **93/93**.
- AOT gate **WAIVED** — env `vswhere`/native-link MSB3073, **0 IL2xxx/IL3xxx** (code AOT-clean), tracked `bd-bo1axj`. Not a regression.

## Test Failure Triage
- In-branch: 10 stale Grpc/RabbitMQ siblings (from ydvyi7/wr3tuo contract change) — **all fixed** (bk59dc, test-side, strengthened).
- Pre-existing: 4 sample CS9003 raw-string breaks (S861 origin) — fixed (13dbkp); 1 orphaned test-host lock (not code).
- Flaky: 0.

## What Worked Well
- **Correctness core landed clean** — payload guard (6×2 surfaces), pepper (fail-open HMAC), LE event, sqlite keyed-init all passed both reviews with the seams pinned at GUIDE holding.
- **Stranded-fix guards held team-wide** — Tests, Backend, and the PM each committed deltas *before* phase-complete; caught 2 stranded deltas (bk59dc pre-phase-complete, CHANGELOG post-carrier-handoff at RETRO).
- **HIVE mesh caught all 3 session deaths** loudly (SA watching Reviewer; Tests flagging Platform) → no silent stalls.
- **Diagnose-open → PM-single-fixer** kept every fix collision-free; grounded challenges refined 3 rulings (RabbitMQ eager-validate = feature not regression; B2 rescope; premise self-corrections).

## What Didn't Work
- **F-5 integration sweep miss** — my payload-contract F-5 grep was swamped by doc-scrub generic-token noise; 10 stale siblings slipped to full-CI. The scrub diff polluted the token extraction.
- **Gate shipped vacuous** — 78z3kq hardcoded `{6}`, blind to ~72% of ids; the self-test only planted 6-char fixtures so it never caught its own gap. Caught at REVIEW_CODE, not authoring.
- **4h18pa "0 tokens" verified by the vacuous gate** — `bd-gblom` (5ch) shipped to CHANGELOG; the two defects were causally linked.
- **3 overnight session deaths** (Platform, DocWriter, Reviewer) — added ~2h of reassign/carrier overhead.

## Recommendations
1. **[CONTINUE] Stranded-fix guard before every phase-complete.** Evidence: caught bk59dc + CHANGELOG deltas that would have reached REVIEW/next-session uncommitted.
2. **[CHANGE] F-5 integration sweep must scope tokens to the *changed src contract*, not the whole diff.** Evidence: doc-scrub noise buried the real ConvertToReceivedMessage siblings. Effort: low — filter f5-sweep token extraction to `src/**` type-contract changes, exclude doc/comment-only files.
3. **[CHANGE] Gate self-tests must plant fixtures across the *full* value domain, not one representative.** Evidence: 78z3kq self-test used only 6-char ids → vacuous on 3-5 char. Generalizes: a non-vacuous self-test enumerates the boundary, not a sample. Effort: low.
4. **[TRY] Author-side gate/scrub verification should not trust a sibling gate that hasn't proven non-vacuity.** Hypothesis: 4h18pa's leak survives only because the gate was blind; requiring the gate's self-test GREEN *before* citing it as a scrub verifier closes that loop.

## Notes for `/self-improving`
- Rule-promotion candidates to weigh (gate-promotion discipline): the **F-5-scope-to-changed-src-contract** clause and **gate-self-test-enumerates-the-domain** clause. Check overlap with `f5-cross-project-test-sweep.md` (F-5) and `enforce-invariants-structurally.md` / `gate-full-guard-suite.md` (non-vacuity) before adding files — likely **clause additions**, not new rules.
