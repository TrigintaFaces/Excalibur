# Retrospective — Sprint 861

**Theme:** Integration Correctness + Advertised-or-Fail-Loud (ADR-336) + MS-First Quality
**Closed:** 2026-06-30 · **Mission 209** · **Final HEAD:** `b245e689` · baseline `a1bac6597` (S860 close)

## Commit Analysis
- **64 commits**, 473 files, +6,735 / −5,446
- fix 17 · test 19 · docs 7 · chore 12 · feat 4 · refactor 3 · ci 2
- Heavy test+fix ratio reflects a correctness/quality sprint (keystone + integration atomicity), not feature build-out.

## Outcome
Full pipeline PLAN→…→DOCS complete. **REVIEW_CODE ✅ + REVIEW_ARCH ✅** @`661cf275`. Governance 5/5, samples SUCCESS, TEST 10/10 shards, VERIFY 0-gaps, AOT IL-clean (native-link waived). 10 beads carried to S862 (all tracked).

## What Worked
- **Adversarial REVIEW + SA-grounding caught real defects, not nits.** The DynamoDb keystone went through 3 grounded corrections before landing: (1) the `// never torn` comment overstated >100 atomicity → fail-fast reject; (2) the guard over-rejected the non-transactional opt-out path → scoped to transactional-only; (3) blanket `TransactionCanceledException`→conflict imprecision → map only on `ConditionalCheckFailed`. Each was a run→read→cite finding.
- **`clean-rebuild-before-trusting-locks` paid off at integration.** `DynamoDbEventStoreBehaviorShould` showed a stale-DLL **false-RED (11/12)** under `--no-build`; a fresh rebuild was 12/12. Catching it pre-commit prevented shipping a "broken" claim on a green change.
- **`enforce-invariants-structurally` for the F2 keystone hardening.** The advertised-strategy guard was made *self-enforcing* — auto-discovers every `*Strategy` enum and asserts exhaustive classification, so a forgotten advertised enum is now inexpressible (RED), not reviewer-dependent.
- **Single-actor stash discipline** resolved a 3-actor RS0016↔RS0025 flap on the held-Cosmos baseline (held set moved as a unit → `stash@{0}`).

## What Didn't / Surprises
- **Stranded TEST/SAMPLES fixes vs committed HEAD.** TEST reported 10/10 GREEN and the mission auto-advanced through VERIFY→SAMPLES→REVIEW — but the DynamoDb regression fix + LE test flip were **uncommitted in worker trees** (the integrator was offline during a gap). The pipeline advanced on a HEAD that wasn't actually green. Caught on integrator re-entry. **Root cause:** phase-completion verified the *working tree*, not *committed HEAD*.
- **F-5 bare-id under-enumeration (recurring class).** F3 cited 2 LeaderElection XML-doc bead-id leaks; the real extent was **~25 files repo-wide** — bare ids (`y6tatp`, `ot72w3`…) the `bd-`-prefixed scrub gate never matched. Same under-enumeration family as prior F-5 retros.
- **A bulk operation tipped a CI gate.** Emptying every project's `PublicAPI.Unshipped` (operator promotion) made the *excluded* `Inbox.CosmosDb` the lone non-empty outlier → baseline audit RED. The exclusion was correct (held stash) but created an inconsistency only the gate caught.
- **AOT native-link environmental recurrence** (`bd-bo1axj`): IL-level AOT clean (0 IL2xxx/3xxx) but native publish fails locally on missing `vswhere`/`link.exe`. Known; passes on CI Linux runner.

## Recommendations
1. **[CHANGE] Phase-completion (TEST/VERIFY/SAMPLES) must verify deltas reached *committed HEAD*, not the working tree.** The integrator commits coupled TEST fixes before the phase task completes. *Evidence:* the stranded DynamoDb/LE fixes advanced the mission on a non-green HEAD.
2. **[CHANGE] Extend the no-internal-refs gate to BARE bead-ids** (not just `bd-` prefix), repo-wide over `///` XML docs. *Tracked:* `78z3kq`. *Evidence:* F3 missed ~23 of ~25 leaks.
3. **[CONTINUE] clean-rebuild-before-trusting + SA-grounded run→read→cite REVIEW challenges.** *Evidence:* caught the stale-DLL false-RED and 3 real DynamoDb correctness nuances.
4. **[TRY] A "held-set integrity" guard** — when a feature set is held (src + PublicAPI baseline + tests), move/stash it as one unit; flag a split (src present, baseline absent). *Hypothesis:* prevents the RS0016↔RS0025 multi-cook flap. *Measurement:* zero split-set build breaks next sprint.

## Test Failure Triage
- In-branch (sprint work): 4 deterministic (volatile, LE last-wins, DynamoDb opt-out ×2) — all fixed.
- Flaky: 1 (`EventStoreLiveSubscriptionDepthShould` 15s→30s) — fixed + the bead reconciled.
- Pre-existing surfaced at CLOSE: 2 CI-audit gaps (AuditLogging baseline, 5 unsharded compat projects) — fixed; bare-id scrub → `78z3kq`.

## Agent Breakdown (praise)
- **TestsDeveloper:** correctly *refused to flip* the #3/#4 DynamoDb tests — caught a real `5wo4w2` regression a flip would have masked.
- **SoftwareArchitect:** two self-retractions (stale-mock mislabel; over-rejection scope) — fast, honest corrections that strengthened the fix.
- **ProjectReviewer:** independent clean-rebuild re-verify on committed mainline; surfaced the keystone completeness gap (F2).
- **PlatformDeveloper:** F2 auto-discovery guard (non-vacuous) + the volatile fix.
