# Just-In-Time Access Sample

Demonstrates time-limited privilege escalation using the A3 Governance JIT access framework. Grants are automatically provisioned and revoked after a configurable TTL.

## What You'll Learn

- Requesting JIT access with justification and duration
- Automatic grant expiry and cleanup
- Audit trail of JIT access events

## Run

```bash
dotnet run
```
