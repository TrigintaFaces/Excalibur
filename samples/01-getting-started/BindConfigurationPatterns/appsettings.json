{
  "ConnectionStrings": {
    "EventStore": "Server=localhost,1434;Database=EventStore;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True"
  },
  "//_EventSourcing_Note": "Bound by Program.cs via sql.BindConfiguration(\"EventSourcing:Sql\"). Keys must match SqlServerEventSourcingOptions property names exactly; a key that does not match binds nothing and fails silently.",
  "EventSourcing": {
    "Sql": {
      "EventStoreSchema": "dbo",
      "EventStoreTable": "EventStoreEvents",
      "SnapshotStoreSchema": "dbo",
      "SnapshotStoreTable": "EventStoreSnapshots"
    }
  },
  "//_Outbox_Note": "Only \"Outbox:Sql\" is bound (Program.cs). Keys must match SqlServerOutboxOptions: schema and table names live on the nested Tables object, processing settings on Processing. PollingInterval has no bindable option -- set it in code with WithProcessing(p => p.PollingInterval(...)).",
  "Outbox": {
    "Sql": {
      "Tables": {
        "SchemaName": "dbo",
        "OutboxTableName": "OutboxMessages"
      },
      "Processing": {
        "DefaultBatchSize": 100,
        "MaxRetryCount": 5,
        "RetryDelayMinutes": 5
      }
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
